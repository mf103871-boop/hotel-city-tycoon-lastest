أكمل HC-P0-S3 من حيث توقفت. التعديلات المنجزة في دوال التمهيد وbaseline-shots.mjs وworkflow تبقى كما هي؛ المطلوب الآن إصلاح أخطاء lint ثم استكمال التقرير والدفع. ممنوع تعديل src أو data أو package.json أو package-lock.json أو playwright.config.ts أو أي توكيد أو منطق اختبار.

1) اعرض الأخطاء أولًا: نفّذ داخل hotel-city-tycoon الأمر npm run lint واطبع الناتج كاملًا حرفيًا مع أرقام الأسطر ومعرّفات القواعد.

2) أصلح الأخطاء السبعة في tools/baseline-shots.mjs بأقل تدخل ممكن ومن دون تعطيل أي قاعدة بتعليقات eslint-disable إلا إذا تعذر غير ذلك، وحينها اشرح السبب في التقرير. إرشادات محددة: إن كانت الأخطاء من نوع no-unused-vars بسبب معامل page غير مستخدم في خطوة فارغة، فاحذف المعامل من تلك الخطوة أو استبدل الدالة الفارغة بدالة لا تأخذ معاملات، بحيث يبقى سلوك السكربت كما هو تمامًا. إن كانت من نوع no-empty-function فاجعل الخطوة الفارغة ترجع Promise.resolve صراحة. لا تغيّر أسماء اللقطات ولا محدداتها ولا ترتيبها ولا منطق إغلاق الهدية.

3) أعد التحقق: npm run typecheck ثم npm run lint وسجّل رمز الخروج لكل منهما. لا تتابع قبل أن يكون الاثنان 0. إن بقي خطأ لا يخص ملفك فتوقف وأبلغ بنصه.

4) استكمل docs/HC-P0-S3-REPORT.md كما هو مطلوب في الخطوة السابقة: أرقام خط الأساس الأولى وهي مخطط 82 ونُفذ 52 ونجح 4 وفشل 48 وإلغاء عند الاختبار 53 لبلوغ سقف الوقت ورفض رفع الحزمة بخطأ 403، ولقطات 1 من 5 نجحت هي 05-phone.png، والسبب الجذري وهو اعتراض طبقة الهدية اليومية للنقرات، ودليله أن اختبار الهدية وحده نجح. أضف فرق التعديلات الكامل وأخطاء lint وإصلاحها. وأضف جدول المؤجل بمعرّفات جديدة يشمل عطل مشغّل PixiJS بعدم تهيئة الshader، وغياب تقرير الخلفية من الconsole، ومخالفة الوضع الصارم لمحدد footer p على الهاتف لمطابقته عنصرين، وتحذير Vite بشأن استيراد public/assets/manifest.json، وانتهاء مهلة لقطة 01-main. حدّث صف P0 في «سجل الحالة الحالي» بالمرجع إلى MEASURED بالأرقام الفعلية مع ذكر أن الاعتماد ينتظر تشغيل CI كاملًا.

5) ادفع بنفس قواعد الأمان: ممنوع طباعة قيمة المفتاح، ومرّر مخرجات الشبكة عبر sed -E 's/(ghp_|github_pat_)[A-Za-z0-9_]*/TOKEN/g':

cd /home/runner/workspace
CLEAN="$(printf %s "${GH_TOKEN:-${GH_TOKEN_}}" | tr -cd 'A-Za-z0-9_')"
git add -A
git commit -m "HC-P0-S3: dismiss daily gift in boot helpers + swiftshader + longer CI budget"
git push "https://x-access-token:${CLEAN}@github.com/mf103871-boop/Hotel-City-Tycoon.git" HEAD:main 2>&1 | sed -E 's/(ghp_|github_pat_)[A-Za-z0-9_]*/TOKEN/g'

اختم بـ«مطابقة التنفيذ مع المرجع» وأسطر نهاية الجلسة الخمسة، وسطر أخير: PUSH_OK أو سبب التوقف بالحرف.

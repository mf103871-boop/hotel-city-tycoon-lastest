اقرأ docs/HOTEL_CITY_MASTER_REFERENCE_AR.md قبل أي عمل. نفّذ الآن HC-P0-S3 وحدها: إزالة حاجز نافذة الهدية اليومية من دوال التمهيد، وتمكين WebGL البرمجي في CI، ورفع السقف الزمني، ثم الدفع. ابدأ بقسم «فحص المرجع» ثم قالب الخطة ثم «مطابقة الخطة مع المرجع». ممنوع تعديل src أو data أو package.json أو package-lock.json أو playwright.config.ts. تصريح مالك محدود: يُسمح بتعديل دوال التمهيد في tests/e2e فقط كما هو مفصل أدناه، وممنوع تغيير أي توكيد أو منطق اختبار. ممنوع تشغيل E2E محليًا على Replit لأن المتصفح معطل هناك بدليل S2 حتى S2E.

خلاصة معتمدة من تشغيل CI الأخير: مخطط 82، نُفذ 52، نجح 4، فشل 48، أُلغي عند الاختبار 53 لبلوغ سقف 40 دقيقة، ورُفض رفع الحزمة بخطأ 403 لأن الوظيفة أُنهيت. السبب الجذري للفشل الجماعي أن الطبقة div الحاملة للصنف z-40 وbackdrop-blur الخاصة بالهدية اليومية تعترض النقرات، والاختبار 25 الذي يتوقع الهدية هو الوحيد الذي نجح.

1) في tests/e2e/game.spec.ts: عدل دالة bootFresh لتقبل معاملًا اختياريًا ثانيًا بالشكل options بقيمة افتراضية كائن فارغ، وبداخله dismissGift بقيمة افتراضية true. بعد اكتمال التمهيد الحالي مباشرة وقبل أن ترجع الدالة قيمتها، وإذا كان dismissGift يساوي true، نفّذ إغلاقًا متسامحًا للهدية: ابحث عن زر باسم مطابق لـ/^collect$|^اجمع/i ضمن الصفحة، وإن ظهر خلال 3000 ملي ثانية فانقره ثم انتظر اختفاء الطبقة بمحدد div.z-40 خلال 5000 ملي ثانية، وإن لم يظهر فتابع بلا خطأ. غلّف كل ذلك بحيث لا يرمي استثناء أبدًا. حافظ على قيمة الإرجاع الحالية للدالة كما هي بالضبط.

2) في نفس الملف: الاختبار الذي عنوانه the daily gift is offered without being hunted for قرب السطر 340 هو الوحيد الذي يجب أن تبقى الهدية ظاهرة فيه، فعدّل نداء التمهيد داخله فقط ليمرر dismissGift بقيمة false. لا تغيّر أي سطر آخر فيه.

3) في tests/e2e/manage.spec.ts: طبّق نفس منطق الإغلاق المتسامح داخل دالة bootRich بعد تمهيدها مباشرة، من دون معاملات إضافية ومن دون تغيير أي توكيد.

4) في tools/baseline-shots.mjs: بعد فتح الصفحة وقبل التقاط أي لقطة، أضف نفس الإغلاق المتسامح للهدية بنفس المحددات والمهل، مرة واحدة لكل صفحة.

5) أعد كتابة /home/runner/workspace/.github/workflows/e2e.yml بحيث تبقى كما هي مع ثلاثة تغييرات فقط: اجعل timeout-minutes تساوي 90، وأضف تحت jobs.e2e مقطع env يحتوي سطرًا واحدًا هو PLAYWRIGHT_EXTRA_ARGS بقيمة --enable-unsafe-swiftshader، وأبقِ كل الخطوات والمسارات كما هي بلا أي تعديل آخر.

6) تحقق محليًا من سلامة التعديلات من دون تشغيل متصفح: نفّذ npm run typecheck ثم npm run lint داخل hotel-city-tycoon وسجّل رمز الخروج لكل منهما. إن فشل أيهما فأصلح خطأك أنت في التعديل فقط، وإن استمر الفشل فتوقف وأبلغ.

7) أنشئ docs/HC-P0-S3-REPORT.md يوثّق: أرقام خط الأساس الأولى كما هي أعلاه، وفرق التعديلات الكامل، وجدول المؤجل بمعرّفات جديدة يشمل على الأقل عطل مشغّل الرسوم PixiJS وتقرير الخلفية المفقود، ومخالفة الوضع الصارم في محدد footer p على الهاتف حيث يطابق عنصرين، وتحذير Vite بشأن استيراد public/assets/manifest.json من JavaScript، وانتهاء مهلة لقطة 01-main. حدّث صف P0 في «سجل الحالة الحالي» بالمرجع إلى MEASURED مع الأرقام الفعلية وذكر أن الاعتماد النهائي ينتظر تشغيل CI كاملًا.

8) ادفع بنفس قواعد الأمان: ممنوع طباعة قيمة المفتاح، ومرّر مخرجات الشبكة عبر sed -E 's/(ghp_|github_pat_)[A-Za-z0-9_]*/TOKEN/g':

cd /home/runner/workspace
CLEAN="$(printf %s "${GH_TOKEN:-${GH_TOKEN_}}" | tr -cd 'A-Za-z0-9_')"
git add -A
git commit -m "HC-P0-S3: dismiss daily gift in boot helpers + swiftshader + longer CI budget"
git push "https://x-access-token:${CLEAN}@github.com/mf103871-boop/Hotel-City-Tycoon.git" HEAD:main 2>&1 | sed -E 's/(ghp_|github_pat_)[A-Za-z0-9_]*/TOKEN/g'

اختم بـ«مطابقة التنفيذ مع المرجع» وأسطر نهاية الجلسة الخمسة، وسطر أخير: PUSH_OK أو سبب التوقف بالحرف.

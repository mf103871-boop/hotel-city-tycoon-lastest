# HC-VIS-001A — تقرير أول دفعة لوضوح العرض

08-09-2026 بتوقيت الأردن / 07-09-2026 UTC — `IMPLEMENTED`، بانتظار معاينة بناء الفرع.

## التنفيذ

- نُقلت الأولوية إلى الجرافيك بموجب DEC-017. فرع مستقل من `main` عند `a77dbf5`؛ لا يعتمد على PR #17 أو #18 ولا يتضمن إعداد توقيع أو رفع متجر.
- أصبحت الكاميرا تحسب حدود الغرف المبنية، بهامش صغير للهيكل والشارع، بدل ملء الشاشة بحدود التوسعة والسماء.
- في الهاتف بالطول يبدأ العرض قرب الاستقبال بمقياس يجعل عرض غرفة 128px نحو 288 CSS px، ما دام ارتفاع الواجهة يسمح. يبقى السحب والقرص متاحين للوصول إلى بقية الفندق.
- يعاد التأطير عند التدوير أو تغير مساحة الواجهة حتى يحرك اللاعب الكاميرا بنفسه. بعد السحب أو القرص أو عجلة الفأرة يُحفظ اختياره مع تطبيق حدود الحركة.
- صار `DecorView` يستجيب لجيل تحميل الأصول؛ وصول الصورة يستبدل BED/SEAT دون انتظار تغيير الليل أو حالة القطعة.

## التحقق المنفذ

| الأمر | النتيجة |
|---|---|
| `npm run test:logic` قبل التعديل | 82 ناجحًا |
| `npm run test:logic -- tests/unit/hotelFraming.test.ts tests/unit/decorRefresh.test.ts` | 10 اختبارات جديدة ناجحة؛ منها اختباران يستخدمان `DecorView` وPixi Container/Sprite الفعليين، مع محمل أصول مضبوط للاختبار |
| `npm run verify` | ناجح: البيانات والأنواع وlint و92 اختبار Vitest والفحوص الذاتية؛ 4 تحذيرات lint قائمة، 0 أخطاء |
| `npm run build` الأول | فشل حد JS بسبب بقايا حزمتين سابقتين في `dist/assets`؛ ليس اعتمادًا لبناء ناجح |
| `npm run mobile:build` | ناجح بعد تنظيف `dist` بأداة المشروع القائمة: JS مضغوط 335KB/350، الفن والصوت 7902KB/12288، 361/361 أصلًا، وفحص غياب مقابض الاختبار ناجح |
| `git diff --check` | ناجح |

التنفيذ المحلي على Node `24.19.0` المتاح في البيئة؛ تحقق Node 22 وتنصيب نظيف وE2E في CI للفرع، وتُضاف النتيجة الفعلية في وصف طلب المراجعة. لا تُنسب اختبارات الجهاز أو الرسم الكامل إلى هذه النتائج.

## ART CHECK والقيود

- تمت مشاهدة القماشة المنشورة قبل التعديل: الفندق صغير والأثاث غير واضح عند التأطير الافتراضي.
- الخادم المحلي يعمل على `127.0.0.1:5173`، لكن متصفح المعاينة أعاد `ERR_BLOCKED_BY_CLIENT` عند محاولة فتحه. لا توجد لقطة تشغيل معتمدة للتغييرات الجديدة؛ لم يُتجاوز الحظر.
- لا أصول PNG جديدة في هذه الدفعة، ولا تغيير في الرسم الكرتوني الحالي أو ملفات الأنيميشن أو الصوت. هذه خطوة وضوح في بداية HC-VIS-001 وليست تسليم العينة المتكاملة.
- اختبار الكاميرا يثبت الحسابات على 390×844 و360×800 و844×390 و1280×800 وحالات حجب الواجهة؛ لا يثبت جودة الفن أو معدل الإطارات على هاتف فعلي.
- لم تتغير ملفات النواة أو البيانات أو الحفظ؛ لا migration. إعادة الحساب عند التركيز/تغيير المساحة فقط، وليست رسمًا جديدًا لكل إطار.

## مطابقة التنفيذ مع المرجع

- عالج التنفيذ وضوح المشهد وART-REFRESH وفق الخطة، بلا انتقال إلى النشر أو الاقتصاد.
- المنطق والبناء متحققان محليًا؛ قبول الشكل والحركة الكاملين لم يتحقق.
- الانتقال إلى تعميم الفن غير مسموح بهذا التقرير. الخطوة التالية داخل العينة: معاينة هذا الفرع وإنتاج/دمج رسم الاستقبال والغرفة الاقتصادية وفق المرجع المعتمد، مع عرض الفرق الفعلي؛ المصعد والحركات التفاعلية لم تُنفذ هنا.

## حالة نهاية الخطوة

1. المرحلة والخطوة: P1/P2، `HC-VIS-001A` ضمن العينة المعتمدة.
2. `VERIFIED` بالدليل: اختبارات التأطير ووصول نسيج الأثاث، والتحقق المحلي والبناء النظيف.
3. `IMPLEMENTED` فقط: المظهر أثناء اللعب بعد التعديل؛ العينة الكبرى HC-VIS-001 ما زالت غير مكتملة.
4. العائق: معاينة بناء الفرع محجوبة في المتصفح المتاح؛ لا مفاتيح Apple مطلوبة لبدء الرسم.
5. الخطوة التالية الوحيدة: معاينة وإكمال فن عينة الاستقبال والغرفة الاقتصادية، دون تعميمها على الكتالوج.

# HC-P2-S2 — الضوء: نهار/ليل متصل، سماء متدرجة، بِرك ضوء داخلية، مسار canvas صادق

**التاريخ:** 20-09-2026
**الحالة:** `IMPLEMENTED` (لا `VERIFIED`: لا قماشة على جهاز بمعالج رسوم رُوجعت — قيد DEC-005/DEC-009؛
ما يمكن إثباته على مسار canvas أُثبت، §5.3)
**الفرع:** `claude/wonderful-dirac-i1mqlk` (خط الأساس `1198f94`)
**المرجع الحاكم:** `docs/HOTEL_CITY_MASTER_REFERENCE_AR.md` v1.3
**الاتجاه البصري:** `docs/ART-0_VISUAL_DIRECTION_AR.md` §10 (الخلفية والسماء)، §11 (لغة الأنيميشن — لم تُمسّ)؛ `docs/HC-VIS-001-SPEC.md`
**وثيقة القرار:** `docs/HC-P2-S2-LIGHT-DECISION.md` (DEC-018، DEC-019)

> كل رقم أو مسار في هذا التقرير مكتوب «يُملأ بعد التحقق» يُملأ من تشغيل فعلي في مرحلة التوثيق،
> ولا يُكتب من الذاكرة ولا من الخطة.

---

## 1. فحص المرجع (قبل أي تعديل)

| ما ينص عليه المرجع | أين | ما فعلته هذه الخطوة |
|---|---|---|
| P2 هدفها ربط كل حالة لعب مهمة بما يراه اللاعب، ويشمل «حالات الليل» | §6 HC-P2 سطر 358-366 | الليل صار حالة متصلة يراها اللاعب كل مساء (غسق 18→20 بالتوقيت المحلي) لا عند الإغلاق فقط؛ الفندق المفتوح ليلًا مبنى مضاء تحت سماء داكنة |
| ART-0 §10: ترتيب الخلفية يبدأ بـ«سماء متدرجة ناعمة» | ART-0 سطر 245 | نُفّذ: تدرج نهاري وليلي مخبوزان وقت التشغيل يتلاشيان بحسب الساعة، مع نجوم وشمس وقمر |
| ART-0 §11: لغة الأنيميشن | ART-0 سطر 258-278 | **لم تتغير في هذه الخطوة**: لا مقطع ولا ورقة ولا ملف حركة مُسّ؛ النجوم والشمس والقمر والبِرك ثابتة لا تتحرك |
| DEC-005: لا اعتماد لميزة مرئية بلا متصفح ودليل بصري | §12 | الدليل لقطات مسار canvas عند ظهر/غسق/ليل (§5.1)؛ السقف `IMPLEMENTED` |
| DEC-006: خطوة واحدة في كل خطة | §12 | خطوة واحدة (الضوء)؛ الهيكل والجسيمات والمصعد مسجَّلة خطواتٍ تاليةً لا منفَّذة |
| DEC-009 / DEC-011: مسار CI يعطّل WebGL؛ «المحرك لا يقلع بلا GPU» | §12 | **صُحّحت الفرضية بـDEC-019**: المحرك يقلع على `CanvasRenderer` تحت المسار نفسه وكان يُبلَّغ عنه `webgl`؛ الشارة والاختبار يقولان `canvas` الآن |
| DEC-017: الحجم مقاس لا مفروض؛ `public/` غير متغير | §12، `tools/selftest/shipping.ts` | لا ملف جديد ولا صورة مُعاد توليدها: `git diff --stat public/ data/` فارغ (يُملأ بعد التحقق)؛ صف `shipping.ts` لم يُمسّ |
| §7: أي مشكلة جانبية تذهب للمؤجل | §7 سطر 523 | BL-039 وBL-040 سُجّلا ولم يُنفَّذا؛ BL-024 وBL-037 أُغلقا لأن ملفيهما يُفتحان لهذه الخطوة أصلًا |
| قاعدة التعديل: لا يُغيَّر ترتيب المراحل إلا بموافقة المالك؛ §7: لا توسيع نطاق صامتًا | §1 سطر 35، §7 سطر 519 | **سؤال للمالك:** صف §11 كان يسمّي الخطوة التالية «S2: مؤثرات التنظيف والتحصيل ونبض الغرفة». قراره «اعتمده على اللعبه الاصليه» قُرئ موافقةً على تبنّي نهج النموذج وبالتبعية على المعرّف HC-P2-S2…S8 بترتيبه (S2 الضوء، S3 الهيكل، S4 المؤثرات ونبض الغرفة). **هل يؤكد المالك هذا الترتيب صراحةً؟** إلى أن يجيب يحمل صف §11 سطرًا مؤرخًا ولا يُعاد كتابته |

**حالة المدخلات عند البدء:** P2 `IN_PROGRESS` عند HC-P2-S1 (`IMPLEMENTED`) وBL-035 مغلق. P1 `IN_PROGRESS`
عند HC-P1-S4 (BL-034 باقٍ). الشجرة نظيفة عند `1198f94` (DEC-017). النموذج `prototypes/next-render`
موجود، خارج `tsc -b` ومتجاهَل في ESLint، ويبقى حتى S6.

---

## 2. الخطة المنفذة

| # | الcommit | ما فيها |
|---|---|---|
| M1 | يُملأ بعد التحقق | الجسر: `daylight.ts` نقي، `selectors.nightAmount`، `stress.ts` يقرأ `?epoch=` |
| M2 | يُملأ بعد التحقق | العارض: `lighting.ts`، `fx/glow.ts`، `lightLayer.ts`، `backdrop.ts` بالتدرج والنجوم والشمس والقمر، `scene.ts`، `characterView.ts`، `app.ts` بخلفية `canvas` |
| M3 | يُملأ بعد التحقق | الواجهة: `HotelCanvas.tsx` (الإزاحة الزمنية، إيقاف الticker عند الإخفاء)، `DebugBadge.tsx`، `App.tsx`؛ `hcstyle.py` وقفة `skyLow` |
| M4 | يُملأ بعد التحقق | الاختبارات: `lighting.test.ts`، فحوص `render.ts`/`characters.ts`/`animations.ts`، `game.spec.ts` |
| M5 | يُملأ بعد التحقق | الحوكمة: وثيقة القرار، DEC-018/019، الصف #20، هذا التقرير، §11/§13/§5A/§14، CI-E2E، MOBILE-DEVELOPMENT، PROJECT-STATE، README |

(إن اختلف عدد الcommits أو ترتيبها عند التنفيذ يُصحَّح الجدول من `git log` لا من هذه القائمة.)

## 3. ما تغيّر فعلًا

### 3.0 أين يراه اللاعب

| الساعة المحلية / الحالة | ما يراه |
|---|---|
| ظهرًا والفندق مفتوح | سماء متدرجة (فاتحة عند الأفق، أزرق أعمق في الأعلى)، شمس، لا بِرك ضوء (الألفا 0 عند `dusk = 0`) |
| 18:00→20:00 والفندق مفتوح | السماء والمدينة والشارع وإطار المبنى تدكن درجةً درجة (24 درجة)، النجوم تظهر من ~19:00، القمر يحل محل الشمس، نوافذ المدينة تُضاء من منتصف المنحدر، بِرك الضوء في الغرف المشغولة واللوبي تشتد |
| بعد 20:00 والفندق مفتوح | الصورة المرجعية: مبنى مضاء (غرف مشغولة ولوبي ببِرك دافئة) تحت سماء داكنة بنجوم وقمر؛ صور الغرف **نهارية** لأن الفندق مفتوح؛ من على الرصيف داكن كالشارع |
| الفندق مغلق (أي ساعة) | كما اليوم بالضبط: كل شيء ليلي، صور الغرف الليلية، غسل الديكور، اللوبي ببركة خافتة (0.35) |
| 05:00→07:00 | المنحدر عكسًا |

### 3.1 الجسر — `src/bridge/daylight.ts` (جديد، نقي) و`selectors.nightAmount`

`localHour(epochMs, tzOffsetMin)` و`clockNight(hour)` و`nightAmountAt(epochMs, tz, closed)`؛ لا استيراد،
لا ساعة، لا Pixi. `nightAmount(state, tz) = isOpen ? clockNight(localHour(state.epochMs, tz)) : 1`.
الإزاحة تقرؤها الواجهة مرة واحدة عند التركيب. `stressRequest` يقرأ `?epoch=` (عدد صحيح غير سالب)
فتُلتقط الأدلة عند ساعة مختارة؛ الجلسة العادية لا تتأثر.

### 3.2 الرياضيات — `src/render/lighting.ts` (جديد، نقي)

`quantiseDusk` (0..24)، `lerpTint` (لكل قناة)، `duskTint(day, dusk) = lerpTint(day, nightfall(day), dusk/24)`،
`poolAlpha`، `starAlpha`. طرفا المنحدر يساويان ألوان اليوم بالبناء، فتكافؤ الليل في `render.ts:727-780` محفوظ.

### 3.3 الأنسجة وقت التشغيل — `src/render/fx/glow.ts` (جديد)

`radialPoolTexture(w, h)` و`verticalGradientTexture(stops)` على canvas خارج الشاشة ثم `Texture.from`؛
كسولة (لا `document` عند الاستيراد)، مخزونة بمفتاح. لا ملفات، لا أقنعة، لا مرشحات، لا RenderTexture.

### 3.4 بِرك الضوء — `src/render/lightLayer.ts` (جديد)

Sprite لكل غرفة مضاءة في `LAYER.overlays` بمزج `add`، من `KeyedPool`؛ يُزامَن من `setSnapshot` بمفتاح
قذر؛ لا عمل لكل إطار. `stats().lights` يعدّ المرئي منها.

### 3.5 الخلفية — `src/render/backdrop.ts`

`update(…, night, dusk)`؛ كل `night ? NIGHT.x : X` صار `duskTint(X, dusk)`؛ السماء sprite واحد بنسيج
متدرج مخبوز لكل درجة غسق (وقفاته `duskTint` لوقفات النهار الثلاث؛ 25 شريطًا 4×256 على الأكثر) — لا
نسيجان يتلاشيان: الصورة هي نفسها والـsprite الثاني بحجم السماء كلفته على مسار Canvas2D إطار كامل
(قيس: 52 fps/p5 30 ليلًا عند `?stress=60` مقابل 59.6/59.5 بالنسيج الواحد؛ §3.3 في قرار الضوء)؛
46 نجمة ببذرة `jitter`؛ شمس وقمر. `SKY_LOW` وقفة جديدة مرآتها `skyLow` في `hcstyle.py`.
ملاحظة «ثلاث حزم لا تدرج» استُبدلت بإحالة إلى DEC-018 وART-0 §10.

### 3.6 المشهد والشخصيات — `scene.ts`، `characterView.ts`

`SceneSnapshot.nightAmount`؛ لون التفريغ `duskTint(SKY, dusk)`؛ `CharacterViewData.dusk` يلوّن من على
الرصيف (`y < 0`) بالمنحدر ومن في الغرف بالمنطقي؛ `drawnSource()` و`characterDiagnostics().source`؛ قصّ
الغرف بلا تخصيص (BL-037) عبر `roomWorldRectInto`.

### 3.7 الخلفية الصادقة — `app.ts` والواجهة

`detectBackend()` يعيد `'canvas'` عند `RendererType.CANVAS`؛ الاتحاد في الأماكن الخمسة؛ الشارة وردية
لـcanvas؛ `preserveDrawingBuffer` تحت `VITE_E2E` فقط (قراءة داخلية، لا لقطة)؛ الticker يتوقف عند
`document.hidden` (BL-024).

### 3.8 ما لم يُمسّ

`src/core/**`، الحفظ (`SCHEMA_VERSION` 20)، `roomView.ts`، `decorView.ts`، `src/render/anim/*.ts`،
`src/render/assets.ts`، `data/**`، `public/**`، ملفات الحركة والأوراق، صف `shipping.ts`، قوائم السماح.

---

## 4. البوابات التي شُغّلت

| البوابة | قبل (`1198f94`) | بعد | ما تثبته |
|---|---|---|---|
| `npm run verify` — vitest | 82 اختبارًا | يُملأ بعد التحقق | + `tests/unit/lighting.test.ts` |
| `npm run verify` — selftest | 804 فحصًا | يُملأ بعد التحقق | + `render.ts` ×3، `characters.ts` ×2، `animations.ts` ×1 |
| `npm run lint` | تحذيرات no-console سابقة فقط | يُملأ بعد التحقق | لا خطأ جديد |
| `npm run build` — الميزانيات (مقاسة، DEC-017) | يُملأ بعد التحقق | يُملأ بعد التحقق | js/css/initial art/audio/all assets مطبوعة؛ الأصول 361/361 |
| `check:cheats` | ✓ | يُملأ بعد التحقق | لا `VITE_E2E` ولا `__hct` في `dist` |
| `git diff --stat public/ data/` | فارغ | يُملأ بعد التحقق | لا ملف مُعاد توليده |
| E2E مسار DEC-009 (`--disable-3d-apis`، بلا التقاط) | يُملأ بعد التحقق (ناجح/فاشل/متخطى، الزمن) | يُملأ بعد التحقق | + «the world is painted» ×2 مشروعين؛ مجموعة التخطي نفسها (تُسمّى)؛ سطر الإقلاع `renderer: canvas` |

فحوص التكافؤ التي يجب أن تبقى بايتًا ببايت: تكافؤ اللوحة (`render.ts:699-725` + `skyLow`)، تكافؤ الليل
(`:727-780`)، `DESIRE` (`:783-826`)، تربية `DecorView` (`:868-886`)، حلقة الشريط الأمامي (`:912-920`)،
`tickAnimation(`/`deltaMS` (`assets.ts:362-368`)، مستمعو DOM (`regressions.ts:302-334`). النتيجة:
يُملأ بعد التحقق.

---

## 5. الدليل

### 5.1 لقطات مسار canvas — `docs/hc-p2-s2-shots/`

| الملف | الرابط المستعمل | ما يجب أن يظهر | النتيجة |
|---|---|---|---|
| `phone-noon.png` (412×915 @2x) | `?stress=12&warm=600&debug=1&epoch=<12:00 محليًا>` | سماء متدرجة وشمس، لا بِرك | يُملأ بعد التحقق |
| `phone-dusk.png` | `…&epoch=<19:00>` | منتصف المنحدر: نجوم أولى، نوافذ مدينة، بِرك بنصف الشدة | يُملأ بعد التحقق |
| `phone-night-open.png` | `…&epoch=<22:00>` | مبنى مضاء تحت سماء داكنة بنجوم وقمر؛ صور غرف نهارية | يُملأ بعد التحقق |
| `phone-night-closed.png` | إقلاع طازج قبل «افتح الفندق» | كما اليوم: صور ليلية، اللوبي بـ0.35 | يُملأ بعد التحقق |
| `desktop-night-open.png` (1280×720) | `…&epoch=<22:00>` | الصورة المرجعية بعرض سطح المكتب | يُملأ بعد التحقق |
| `webgl-attempt.md` | المسار الافتراضي (SwiftShader) | **ملاحظة بيئة** (BL-020)، لا دليل | يُملأ بعد التحقق |

الإزاحة الزمنية في الصندوق: يُملأ بعد التحقق (UTC متوقعة؛ تُذكر قيمة `epoch` لكل لقطة).

### 5.2 أرقام الصندوق — تُسمّى أرقام صندوق

`window.hct.perf()` عند `?stress=60&warm=900&debug=1` على مسار canvas (Chromium برمجي، بلا GPU):
يُملأ بعد التحقق (fps الوسيط وp5، عدد الأشخاص المرسومين/الكلي من `hct.characters()`، `stats().lights`).
هذه **ليست** قياس هاتف ولا قياس WebGL؛ لا تُقارن بحد p5 ≥ 55fps.

نمو ذاكرة التلوين على Canvas2D أثناء منحدر ظهر→ليل: يُملأ بعد التحقق (المتوقع: لا نمو بعد 25 درجة).
مدد الاختبارات على المسار بمقرر القائمة مقابل خط الأساس: يُملأ بعد التحقق.

### 5.3 لماذا `IMPLEMENTED` وليس `VERIFIED`

ما تعد به هذه الخطوة أن يراه اللاعب — الليل يهبط على المدينة ببطء والفندق يضيء من الداخل — يُرى في
لقطات مسار canvas في §5.1، وهي أول لقطة محرك حقيقية في P2 (لا تركيبًا حسابيًا كما في S1). لكن مسار
canvas يثبت أن Pixi يرسم ما وُصف على Canvas2D **في صندوق بلا معالج رسوم**؛ لا يثبت أن الصورة على
شاشة هاتف حقيقي مقبولة عند عين المالك، ولا أن p5 ≥ 55fps عند 60 غرفة و55 شخصًا. المحاولة على WebGL
البرمجي (SwiftShader) بيئة تتجمد (BL-020) ولا تُعرض دليلًا. الطريق الوحيد إلى `VERIFIED` هو إجراء
`docs/MOBILE-DEVELOPMENT.md` على هاتف مع طراز الجهاز ومعرّف البناء (DEC-005/DEC-009).

---

## 6. ما لم يُنفذ — سُجّل ولم يُلمس (§7)

| المعرّف | البند | الحالة |
|---|---|---|
| BL-024 | حلقة الرسم لا تتوقف عند إخفاء الصفحة | `CLOSED` هنا |
| BL-037 | `scene.render` يخصّص مصفوفتين لكل إطار في قصّ الغرف | `CLOSED` هنا |
| BL-036 | مصعد مرسوم بحركة باب | مجدول HC-P2-S5 (داخل اللوبي كما في المرجع) |
| BL-039 | تلاشٍ متقاطع بين صورة الغرفة النهارية والليلية (اليوم تبديل فوري عند الإغلاق) | جديد، منخفضة |
| BL-040 | مقبض قياس أداء أصلي داخل Capacitor (S8) | جديد، متوسطة |

خارج النطاق أيضًا: الهيكل (S3)، الجسيمات ونبض الغرفة (S4)، parallax وحياة الشارع (S6)، الحوادث الحية (S7)،
BL-023، BL-034، ART-REFRESH (HC-VIS-001 بوابة 1).

---

## 7. مطابقة التنفيذ ↔ المرجع

| بند بوابة P2 / شرط قبول | الحالة | الدليل |
|---|---|---|
| حالة الليل لها تمثيل مرئي متصل يظهر ضمن الزمن المتوقع | يُملأ بعد التحقق | `phone-dusk.png`، `phone-night-open.png`؛ `nightAmount` حتمي (`characters.ts` selftest) |
| الفندق المغلق يبدو كما اليوم بالضبط | يُملأ بعد التحقق | `phone-night-closed.png`؛ `duskTint(x, 24) === nightfall(x)` |
| كل لون ليلي عبر `nightfall()`؛ لا لون أفتح من نهاريه في أي درجة | يُملأ بعد التحقق | `render.ts`: «the sky is one continuous night from noon to midnight» |
| تكافؤ اللوحة مع `hcstyle.py` بما فيها `skyLow` | يُملأ بعد التحقق | `render.ts:699-725` |
| الخلفية تُسمّى بصدق في الأماكن الخمسة | يُملأ بعد التحقق | `render.ts`: «the renderer names every backend it can actually get»؛ سطر الإقلاع في E2E |
| كل شخص مرسوم من ورقة لا كبسولة | يُملأ بعد التحقق | `game.spec.ts` «characters are drawn» + `source !== 'none'` |
| القماشة مرسومة لا فارغة (≥98% معتم، ≥8 ألوان، لا لون >70%) | يُملأ بعد التحقق | `game.spec.ts` «the world is painted, not left blank» (مسار canvas) |
| لا `Math.random` ولا DOM عند الاستيراد في وحدات العرض النقية | يُملأ بعد التحقق | `animations.ts` فحص النقاء |
| قصّ الغرف بلا تخصيص | يُملأ بعد التحقق | `render.ts`: «the room cull allocates nothing» |
| لا تغيير في النواة أو الحفظ | ✅ | `SCHEMA_VERSION` 20؛ لا حقل في `GameState`؛ `replay.test.ts` بلا تعديل |
| reduced-motion: لا شيء جديد يتحرك | ✅ | النجوم/الشمس/القمر/البِرك ألفا ثابتة؛ `accessibility.ts` بلا تغيير |
| لا ملف جديد في `public/`؛ الحجم مقاس (DEC-017) | يُملأ بعد التحقق | `git diff --stat public/ data/` فارغ؛ الميزانيات مطبوعة |
| **الأداء p5 ≥ 55fps عند 60 غرفة و55 شخصًا** | ⚠️ | يحتاج هاتفًا (§5.3) |
| **مراجعة بصرية على جهاز حقيقي** | ⚠️ | DEC-005/DEC-009: على المالك بعد النشر |

### ART CHECK

- **هل رُسم فن جديد بيد Fable؟** لا ملف. السماء والبِرك أنسجة تُولَّد وقت التشغيل من أرقام `hcstyle.py`
  عبر `nightfall()`؛ لا placeholder، ولا أصل من خارج المستودع، ولا صورة مُعاد توليدها. الوقفة الوحيدة
  المضافة (`skyLow`) اسم في اللوحة لا يُرسم في أي PNG.
- **المرجع المعتمد مقابل `phone-night-open.png`:** يطابق — سماء داكنة، غرف مضاءة، لوبي مضاء، حدود سوداء
  تتبع الليل، باستيل. ينتظر — الهيكل (S3)، الجسيمات (S4)، المصعد (S5)، العمق (S6)، الحوادث (S7).
  الفروق المرصودة: يُملأ بعد التحقق.
- **HC-VIS-001 سطر 24 (اللوحة تُعاير في لقطة المحرك):** سماء `hcstyle` `#6FBCF9` مقابل `#6CCDEB` في العقد؛
  الفرق يُسجَّل ولا يُعاير هنا. ملاحظة القياس: يُملأ بعد التحقق.
- **هل احتُرمت لغة ART-0 §11؟** لم تُمسّ: لا مقطع ولا إطار ولا سرعة تغيّرت.

---

## 8. سطور نهاية الجلسة (§16)

1. **المرحلة والخطوة:** HC-P2 «جعل الفندق حيًا بصريًا» — الخطوة HC-P2-S2 «الضوء». P1 ما زالت
   `IN_PROGRESS` (BL-034).
2. **ما أصبح `VERIFIED`:** لا شيء جديد. ما يمكن إثباته في صندوق بلا GPU أُثبت — الرياضيات، التكافؤ،
   رسم Pixi على Canvas2D، ولقطات المسار — لكن أيًّا منها ليس عين المالك ولا قياس هاتف.
3. **ما بقي `IMPLEMENTED` فقط:** HC-P2-S2 كلها. `npm run verify`: يُملأ بعد التحقق؛ `npm run build`:
   يُملأ بعد التحقق؛ E2E مسار DEC-009: يُملأ بعد التحقق.
4. **ما ينتظر المالك:**
   - **تأكيد صريح** لمعرّف الخطوات HC-P2-S2…S8 وترتيبها (المؤثرات ونبض الغرفة إلى S4) — §1.
   - **مراجعة بصرية على الهاتف بعد النشر:** افتح Pages بعد 18:00 محليًا أو بـ`?epoch=<ms ليلًا>` وانظر
     إلى منحدر الغسق، النجوم والقمر، الغرف المضاءة واللوبي؛ ثم `?stress=60&warm=900&debug=1` واقرأ p5
     من الشارة أو `window.hct.perf()` (`docs/MOBILE-DEVELOPMENT.md`). p5 ≥ 55 هو شرط `VERIFIED`.
5. **الخطوة التالية الوحيدة:** HC-P2-S3 — خبز الهيكل في أوراق الشخصيات (رأس 54%، مشي بالمسافة بحد 12fps)
   مع إعادة توقيع بند الإطارات في الصف #19 صفًا #21. لا تُنفَّذ تلقائيًا، وتبدأ بـ`pip3 install pillow`
   ووثيقة قرار.

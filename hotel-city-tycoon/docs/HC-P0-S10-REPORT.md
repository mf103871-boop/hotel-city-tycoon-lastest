# HC-P0-S10 — تقرير أساس الهاتف

التاريخ: 07-09-2026. الفرع المحلي: `codex/mobile-foundation-2026-09-07`.
خط الأساس: `1b05c1562f51c59a1652f3994c6a52169c57afca`.
الحالة: `IMPLEMENTED` — التحقق المحلي الآلي ناجح؛ مراجعة الهاتف والمتصفح للتغييرات لم تكتمل.

## ما نُفذ

- ثُبتت قرارات المالك الثلاثة والخطة الرسمية v1.0، والمرجع v1.3 مع DEC-015/016، والصورة المعتمدة وبصمتها ومواصفات عينة HC-VIS-001.
- أُضيف Capacitor 8.5.1 بمشروع Android ومشروع iOS يستخدم SPM، مع أصول ويب مجمعة محليًا وهوية تطوير مؤقتة `com.hotelcitytycoon.app`.
- أضيفت أوامر mobile:build / mobile:sync / mobile:android / mobile:ios ودليل البناء والاختبار.
- إعداد iPhone يسمح بالطول والجانبين؛ Android يتبع تفضيل تدوير النظام؛ manifest الويب أصبح `any`.
- مساحة آمنة مشتركة لأربع حواف تشمل القماشة والأدوات والقوائم، وتقبل قيم SystemBars على Android. أُزيلت الإزاحة المكررة من HUD.
- القوائم تستخدم جسمًا مرنًا قابلًا للتمرير، مع مساحة أكبر عند قصر الشاشة بالعرض؛ زر الإغلاق 44px على الأقل. ResizeObserver يتابع تغير مساحة اللعبة.
- نسخة الهاتف تُبنى بمسار `/` ومن دون تسجيل Service Worker أو مقابض الاختبار؛ مُخرج dist يُنظف قبل البناء لمنع نسخ ملفات قديمة.
- أضيف اختبار E2E لتدوير الشاشة والقائمة مفتوحة وثبات قراءات المال/المستوى والوصول إلى آخر صف، وأصبح workflow معدًا للعمل على PR مع verify وmobile:sync. **الاختبار الجديد وCI لم يُشغّلا في هذه الجلسة.**

لم تُغير المحاكاة أو أرقام التوازن أو schema 18 أو الرسومات المشحونة في اللعبة. أيقونات وشاشات انطلاق المشروعين الأصلية ما زالت قوالب Capacitor للتطوير. لم يُنتج رسم أو أنيميشن العينة بعد.

## التحقق الفعلي

| الإجراء | النتيجة |
|---|---|
| `npm run verify` بعد التغييرات | البيانات والأنواع ناجحة؛ 82 Vitest في 5 ملفات؛ 776 فحصًا ذاتيًا؛ lint بلا أخطاء وبـ4 تحذيرات console القائمة |
| `npm run mobile:sync` | بناء إنتاج ناجح ونسخ/مزامنة Android وiOS ناجحة |
| حراس البناء | JS gzip نحو 335KB من 350KB؛ الفن والصوت 7902KB من 12288KB؛ جميع 361 أصلًا موجودة؛ إزالة 10 sourcemaps وعدم وجود مقابض اختبار |
| فحص محلي مباشر | فُحص Info.plist وAndroidManifest، وعدم وجود server.url، ومطابقة بايتات كل ملف dist مع نسخته في المشروعين، وبصمة الصورة المعتمدة — ناجح |
| `git diff --check` | ناجح |
| فتح النسخة المحلية في المتصفح المتاح | تعذّر: `net::ERR_BLOCKED_BY_CLIENT` على `http://127.0.0.1:5000/`؛ لا لقطة جديدة أو اعتماد بصري للتعديلات |
| بناء APK/IPA أو تشغيل جهاز | لم يُشغّل؛ البيئة Linux وبها Java 17، لا Xcode ولا Android SDK ظاهر. إنشاء المشروع ومزامنته ليسا بناءً أصليًا |

ملاحظات التنفيذ: محاولة Vite الأولى على `0.0.0.0` فشلت بخطأ `uv_interface_addresses`؛ التشغيل على `127.0.0.1` نجح. أول بناء للهاتف وجد ملفات JS قديمة في dist فأظهر 669KB وفشل حارس الميزانية؛ بعد تنظيف المخرجات عاد القياس إلى 335KB، وأُضيف التنظيف إلى أمر الهاتف. رصد lint استيراد URL الناقص في أداة البناء الجديدة؛ أُصلح وأُعيد verify. لم تُرفع أي ميزانية لتجاوز فشل.

## الرفع والمراجعة

توقفت محاولة الرفع الأولى بسبب غياب مصادقة Git ثم رفض اتصال GitHub إنشاء الفرع برسالة `403: Resource not accessible by integration`، وحُفظت حزمة patch في حينها. بعد أن أكمل المالك تثبيت التطبيق ومنحه الوصول في 07-09-2026، نجح إنشاء الفرع ورفع التعديلات الـ98 عبر اتصال GitHub، وفُتح [طلب المراجعة المسودة #16](https://github.com/mf103871-boop/hotel-city-tycoon-lastest/pull/16).

التزام التنفيذ المرفوع `b81bf2960e0c01effe30b2768891ef339105fd8e` يطابق شجرة النسخة المحلية المختبرة تمامًا: `571e933f38604a5ce6cb1bdcde2ed64449d16afb`. بدأ [تشغيل CI #18](https://github.com/mf103871-boop/hotel-city-tycoon-lastest/actions/runs/34154658415) ووصل إلى verify ومزامنة المنصتين عند كتابة هذا التحديث؛ لا تُحسب اختبارات E2E أو اللقطات ناجحة قبل اكتمالها. لم يُدمج الطلب ولم يُنشر تطبيق في المتاجر.

## مطابقة التنفيذ مع المرجع

- إعداد المنصة بناءً على طلب المالك «من البداية»، بلا تغيير ترتيب إنتاج الفن أو الاقتصاد.
- عقد العينة جاهز؛ الصورة المرجعية خارج public ولم تتحول إلى خلفية بديلة عن أصول قابلة للتحريك.
- لا ترحيل حفظ أو تغيير معرّفات؛ انتقال حفظ الويب إلى أصل التطبيق لا يحدث تلقائيًا.
- لا ادعاء أداء جهاز أو جاهزية متجر أو VERIFIED للواجهة. تبقى P1 وP2 `IN_PROGRESS`.
- قبول الخطوة كاملًا ينتظر فحص المتصفح/الهاتف وأول بناء أصلي؛ تم تجهيز الاختبارات ومسار المراجعة ولم تُحسب على أنها نجحت.

## حالة نهاية الجلسة

1. المرحلة والخطوة: P0 / HC-P0-S10 — أساس التنفيذ على الهاتف.
2. المتحقق بالدليل: الخطة والصورة وعقد العينة، verify، بناء الويب الأصلي ومزامنة ملفات المنصتين ومطابقتها.
3. `IMPLEMENTED` فقط: غلافا التطبيقين ودعم التدوير والمساحات الآمنة قبل تشغيلهما على الهاتف والتحقق البصري.
4. عائق صلاحية GitHub حُلّ، والفرع وطلب المراجعة متاحان. هوية المتاجر والتوقيع تُطلب عند تجهيز أول توزيع.
5. الخطوة التالية: استكمال تحقق HC-P0-S10 على مسار مراجعة قابل للتشغيل، ثم عينة HC-VIS-001 حسب العقد؛ لم يبدأ إنتاجها في هذه الجلسة.

## متابعة 07-09-2026 — فشل الإغلاق في اختبارات CI

- كشف CI #19 على `0ed3fe9` فشلًا حقيقيًا في تحديث الاختبارات: الاسم المتاح لزر Sheet أصبح `Close`، لكن سبعة محددات في `game.spec.ts` و`manage.spec.ts` بقيت تبحث عن `✕`. فشلت ثلاثة سيناريوهات على كل من desktop وphone: التنقل بين لوحات الشريط، إعادة الديكور للمخزن، وبيع قطعة مخزنة.
- النتيجة السابقة الدقيقة: 68 ناجحًا، 6 فاشلة، 12 متخطاة في 6.6 دقائق؛ verify وبناء/مزامنة المنصتين واللقطات اكتملت بنجاح. اختبار التدوير والمساحات الآمنة نجح على المقاسين.
- الإصلاح: تحديد `Close` داخل `dialog` في المواضع السبعة، مما يختبر الزر المرئي ويحافظ على اسمه لقارئات الشاشة ويتجنب زر الخلفية ذي الاسم نفسه. لم تُحذف تأكيدات أو تُرفع مهل أو تُضف شروط تخطٍ.
- التحقق المحلي: ESLint للملفين وفحص whitespace ناجحان؛ Playwright اكتشف السيناريوهات الثمانية المستهدفة (ثلاثة سيناريوهات فاشلة سابقًا والتدوير، على مشروعين). الاكتشاف ليس تشغيلًا، وChromium الخاص بـPlaywright غير مثبت محليًا؛ اعتماد التنفيذ ينتظر إعادة CI على الإصلاح.
- مطابقة التنفيذ: متابعة محدودة لـHC-P0-S10، بلا تغيير للكود المشحون أو الرسم أو الحفظ أو الاقتصاد. الإصلاح `IMPLEMENTED` حتى انتهاء CI الجديد؛ اعتماد الهاتف الأصلي ما زال مطلوبًا.

## نتيجة إصلاح E2E ثم متابعة البناء الأصلي

نجح [CI #20](https://github.com/mf103871-boop/hotel-city-tycoon-lastest/actions/runs/34156358695) على `594bb64`: 74 E2E ناجحة، صفر فشل، 12 متخطاة بالشروط السابقة، في 2.2 دقيقة. نجحت verify والمزامنة واللقطات أيضًا. إصلاح محددات الإغلاق `VERIFIED`، ودُمج PR #16؛ main عند بدء متابعة البناء هو `a77dbf5`.

أُضيف مسار `native-build` لإخراج APK تطوير ونسخة iOS Simulator مع فحص الحزم والسجلات. حالته عند كتابة الخطة `IMPLEMENTED` حتى تشغيل Gradle وXcode فعليًا. توثق النتيجة النهائية وروابط artifacts في طلب المراجعة لهذه المتابعة؛ لا تُعتمد من وجود workflow وحده.

1. المرحلة والخطوة: متابعة البناء الأصلي ضمن P0 / HC-P0-S10.
2. المتحقق: إصلاح E2E وverify والبناء الويب والمزامنة وفق CI #20.
3. قيد التحقق: بناء APK ومحاكي iOS؛ ثم تشغيل الأجهزة والقبول البصري والأداء.
4. القرار المتبقي قبل توزيع iPhone: هوية المتجر وتوقيع Apple، بعد وجود بناء قابل للمراجعة.
5. الخطوة التالية: تجربة النسخ الأصلية على أجهزة بعد نجاح البناء، قبل اعتماد أساس الهاتف.

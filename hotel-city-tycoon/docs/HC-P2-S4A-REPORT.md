# HC-P2-S4A — تقرير خطوة «كبر الرقم» (DEC-024، الصف الموقّع #23)

**التاريخ:** 21-09-2026 · **الحالة:** `IMPLEMENTED` (سقف DEC-005/DEC-009) ·
**القرار:** `docs/HC-P2-S4A-LABEL-SCALE-DECISION.md` · **الدليل:** `docs/hc-p2-s4a-shots/`
· **الأساس:** `d41d96c` (HC-P2-S4)

الخطوة **تصحيح داخل نطاق HC-P2-S4 بطلب المالك المباشر**، لا رتبة في سلّم
DEC-018 §6. الخطوة التالية تبقى **HC-P2-S5** بقرارها المحجوز DEC-022.

---

## 1. فحص المرجع (قبل أي تعديل)

الجدول كاملًا في `docs/HC-P2-S4A-LABEL-SCALE-DECISION.md` §0. ما يهم هنا سطران:

- **ART-0 §13 سطر 336** «لا نعتمد على تفاصيل أصغر من بكسلين فعليين بعد
  التصغير» — **كان مخروقًا مقيسًا** منذ S4 للرقم العائم، ولم يُذكر في فحص
  مرجع S4. الأرقام في §4.
- **DEC-021** «الصورة المرسومة دالة نقية من (معاملات الإطلاق، الخطوة)» —
  يُخصَّص للرقم وحده بإلحاق مؤرخ، ولا يُلغى ولا يُعاد كتابته.

**انحراف مسجَّل:** وثيقة القرار كُتبت **بعد** الكود لا قبله، خلافًا للصف #20.
مكتوب في رأسها ولا يُبرَّر.

---

## 2. ما نُفّذ

**ما كان:** الرقم العائم `+N` مرسوم في فضاء العالم. عند التكبير الذي يفتح
عليه الهاتف اللعبة — 0.40×، وهو أرضية الكاميرا — كان «+25» كلّه **12 × 4.8
بكسل CSS**: لطخة ذهبية لا رقمًا. هذا اكتشاف قاسه تقرير S4 نفسه ووضع خياراته
الثلاثة على المالك، فأجاب: **«كبر الرقم»**.

**ما صار:** الرقم يُرسم **بحجمه في العالم أو بـ24 بكسل CSS على الشاشة أيّهما
أكبر، ولا يتجاوز نصف طابق في العالم** — الأصغر من السقفين نافذ.

| | قبل | بعد |
|---|---|---|
| المقياس `k` عند 0.40× | 1 | **4** |
| ارتفاع الشكل | 4.8 بكسل CSS (9.6 جهاز) | **19.2 بكسل CSS (38.4 جهاز)** |
| «+25» كاملًا (بخطوة الأشكال، اصطلاح تقرير S4) | 12 × 4.8 بكسل CSS | **48 × 19.2 بكسل CSS** |
| الصعود | 24 بكسل عالم (9.6 CSS) | **24 بكسل عالم (9.6 CSS)** — بلا تغيير |
| عند `zoom ≥ 2` (تكبير الغرفة 2.02× و3×) | — | **`k = 1` بالضبط: لا شيء يتغيّر** |

ثلاثة قرارات تحمل الخطوة، وكلّها من مراجعة عدائية لا من ذوق:

1. **الحجم وحده يُقاس بالشاشة؛ الصعود يبقى في العالم.** الحجم يربط الرقم
   بالعين، والصعود يربطه بالغرفة. ضربُ الصعود في `k` قيس يفعل خطأين: يصعد
   الرقم 120 بكسل عالم أمام طابق 96 فيُرسم فوق سطح المبنى بعيدًا عن الغرفة
   التي دفعت، ويعود صاعدًا عبر بطاقة ردّ الفعل التي أنزله دونها إصلاح S4 §9.
   بتركه في العالم تسلك حافّة الرقم العليا **المسار نفسه الذي سلكته قبل هذه
   الخطوة عند كل تكبير**، فلا يحتاج إجلاسُه تحت البطاقة حسابًا جديدًا.
2. **سقف نصف الطابق.** `LABEL_SCALE_ROOM_MAX = BLOCK_H / (2 × GLYPH_H) = 4`،
   وهو النافذ اليوم دون سقف الشاشة (5). بلا هذا السقف يصير «+25» عند 0.40×
   **145 بكسل عالم عرضًا أمام غرفة عرضها 128** — لافتة أعرض من الغرفة التي هي
   عنها. ومعه يصير 116 بكسل عالم (91% من عرض غرفة).
3. **المقعدان يثبّتان حافّة لا مركزًا.** الـSprite مرتكزه مركزه فيكبر نزولًا
   بقدر ما يكبر صعودًا، ونقطة الارتكاز أعلى رأس الشخص. بلا بطاقة تثبت الحافة
   **السفلى** فينمو الرقم صاعدًا خارج الشخص؛ ومع بطاقة تثبت الحافة **العليا**
   فيبقى تحتها. وعند `k = 1` ترتدّ القاعدتان إلى ما شُحن حرفيًا.

**وعلّة سابقة أُصلحت في الطريق:** `freeLabel` كان يُسقط **ستّ دفعات من اثنتي
عشرة** في الدفعة الواحدة. دفعة اللحاق تُشغّل حتى `MAX_CUES_PER_BATCH` إشارة في
حلقة واحدة قبل أي `tick`، فكل لافتة حيّة عمرها صفر، والمقارنة `>` لا تصدق بين
متساويين — فيعود المسح إلى الخانة 0 في كل مرة وتدوسها الإشارات 7..12 بالتتابع.
صار لا يُعاد استعمال خانة لم يُرسم رقمها بعد: دفعة لا تُرى خير من دفعة تمحو
أخرى صفرَ إطارات. علّة من S4، أُصلحت هنا لأن أسوأ حالاتها هي بعينها ما طُلب من
هذه الخطوة أن تفكّر فيه.

---

## 3. الملفات

| الملف | ما تغيّر |
|---|---|
| `src/render/fx/particles.ts` | `LABEL_SCREEN_PX` و`LABEL_SCALE_STEPS` و`LABEL_SCALE_ROOM_MAX` و`LABEL_SCALE_MAX` و`labelScaleFor(zoom)`؛ `labelOriginX` يأخذ المقياس؛ `labelRiseOf` **لا يأخذه** ووثيقته تقول لماذا |
| `src/render/fx/particleLayer.ts` | `setZoom(zoom)`؛ خانة `Label` تحمل المرساة (`wx`/`wy`/`dropped`/`dropY`) بدل موضع مرصوف؛ `layoutLabel` و`labelBaseY`؛ `stats()` يبلّغ `labelScale` و`labelScaleDrawn`؛ إصلاح `freeLabel` |
| `src/render/scene.ts` | `fx.setZoom(camera.zoom)` قبل `fx.tick` مباشرة؛ `fxStats()` يمرّر الرقمين |
| `tools/selftest/effects.ts` | فحص جديد كامل (الوحدة من 16 إلى 17 فحصًا)، وحارس «لا تخصيص في الإطار» صار يقرأ **خمسة** أجسام بدل اثنين |
| `tests/unit/effects.test.ts` | أربع حالات جديدة للمقياس وسقوفه وشبكته وسقف الغرفة، وحالة لرصف رقم مكبَّر، وحالة لتكبير الأطلس، وتأكيد أن الصعود لم يعد يأخذ مقياسًا؛ **35 → 42** حالة في الملف |
| `tests/e2e/game.spec.ts` | تأكيدان داخل «the effects put pixels on the canvas»: `labelScale > 1` و`labelScaleDrawn === labelScale`؛ ونوع `FxStats` يحمل الحقلين |

**لا شيء** في `src/core` ولا `src/bridge` ولا `public/` ولا `data/` ولا
`SCHEMA_VERSION` (يبقى 20) ولا الأطلس (`ATLAS_RES` يبقى 2).

---

## 4. الأرقام المقيسة

### 4.1 الهندسة — مشتقّة من ثوابت مثبّتة ومضروبة في مقياس مقروء من اللعبة وهي تعمل

| التكبير | `k` | ارتفاع الشكل CSS | عرض **حبر** «+25» CSS | ارتفاعه في العالم |
|---|---|---|---|---|
| 0.40× (أرضية الكاميرا، ما يفتح عليه الهاتف) | 4 | **19.2** | 46.4 | 48 = نصف طابق |
| 0.50× | 4 | 24.0 | 58.0 | 48 |
| 1.00× | 2 | 24.0 | 58.0 | 24 |
| 2.00× | 1 | 24.0 | 58.0 | 12 |
| 2.02× (تكبير لقطات S4) | 1 | 24.24 | 58.6 | 12 |
| 2.50× (سقف الكاميرا) | 1 | 30.0 | 72.5 | 12 |

**اصطلاحان للعرض، وكلاهما مكتوب:** «بخطوة الأشكال» `n × GLYPH_ADVANCE_PX × k × zoom` وهو ما اقتبسه تقرير S4 (12 بكسل CSS قبل، 48 بعد)، و«حبرًا» `((n-1) × GLYPH_ADVANCE_PX + GLYPH_W) × k × zoom` وهو ما يُقارن بعرض الغرفة (46.4 بكسل CSS عند 0.40×، أي 116 بكسل عالم أمام 128). الفرق شكل واحد ناقصًا خطوة، لا خلاف في قياس.

الشكل **لا ينزل تحت 24 بكسل CSS عند أي تكبير يبلغه التثبيت** (حتى 0.5×)، وهذا
يثبته فحص ذاتي يمشي المدى كلّه بخطوة 0.0007 — ويصحّ **فقط** لأن التكميم للأعلى.

### 4.2 خرق ART-0 §13 وإغلاقه — عند 0.40× ودقة جهاز 2

| التفصيلة | قبل | بعد | الأرضية الموقّعة |
|---|---|---|---|
| قلم الشكل `PEN_W` 1.8 | **1.44 بكسل جهاز** | **5.76** | 2 |
| الحافة الصلبة `SOLID_RIM_W` 1 | **0.80 بكسل جهاز** | **3.20** | 2 |
| بطاقة ردّ الفعل (لم تُكبَّر) | 0.80 | **0.80** | 2 — **ما زال مخروقًا، BL-053** |

### 4.3 قياس البكسل — وما لا يعزله

على عالم ساكن فعلًا (`?stress=0`)، والدليل على سكونه أن عدد البكسلات المتغيّرة
يعود **صفرًا** بعد انتهاء كل شيء في الذراعين. الصندوق نفسه (140 × 108 بكسل CSS)
والشخص نفسه (`g211`، وهو الشخص الذي أطلقت عليه الإشارة فعلًا لا شخصٌ قريب) في
الذراعين:

| عند 40ms | قبل | بعد |
|---|---|---|
| بكسلات ذهبية | **1176** | **1729** |
| بكسلات متغيّرة | **1870** | **3366** |
| أرضية الضجيج (بعد 490ms) | 0 | 0 |

**وما لا تعزله هذه الأرقام**، مكتوبًا لا مضمَرًا: تقليل الحركة يطلق عملة واحدة
في مكانها بدل خمس مسافرة، و`playDebugCue` يومض إطار الغرفة كذلك — فالبكسلات
المعدودة هي الرقم **وعملة واحدة ووميض الإطار**. الرقم الذي يُقاس بلا لبس هو
الهندسة في §4.1، والصورة في §5.

### 4.4 انحرافات التنفيذ عن نص الطلب

| البند | الحال |
|---|---|
| «كبر الرقم» يُقرأ الخيار 1 بآلية الخيار 2 | **قراءة لا نصّ**، قابلة للتصحيح بإلحاق مؤرخ (§1 من وثيقة القرار) |
| عند تكبير الغرفة 2.02× الرقم **لا يكبر** | مقصود: هناك وُصف بـ«مقروء بلا جهد»، والتكبير هناك يفسد ما لم يُشكَ منه |
| عند 0.40× الشكل 19.2 لا 24 | سقف نصف الطابق هو النافذ، والسبب في §2 بند 2. مكتوب في الثابت نفسه وفي DEC-024 |
| البطاقة والعملة لم تُكبَّرا | نطاق: المالك قال «الرقم». BL-053 وسؤال صريح |

---

## 5. التشغيل

| البوابة | النتيجة | السجل |
|---|---|---|
| `npm run verify` | **الخروج 0** — 172 اختبار vitest في 8 ملفات (165 قبلها)، **818 فحصًا ذاتيًا في 32 وحدة** (817 قبلها)، lint **0 خطأ / 4 تحذيرات** سابقة نفسها | `scratchpad/s4a-verify2.log` |
| `npm run build` | **الخروج 0** — js **366KB gzip بلا تغيير**، css 7KB، الأصول **361/361**، `check:cheats` نظيف | `scratchpad/s4a-build2.log` |
| E2E مسار DEC-009 | **82 ناجحًا / 0 فاشل / 12 متخطاة في 3.1 دقيقة** — العدد نفسه كخط أساس S4، والمتخطاة هي الاثنتا عشرة نفسها **بالاسم** | `scratchpad/s4a-e2e3.log` |
| `git diff -- public data` | فارغ | — |

**حدّ صدق على هذه الدورة:** شُغّل E2E أولًا **بلا** `PLAYWRIGHT_EXTRA_ARGS=--disable-3d-apis`
فسقط منه أحد عشر اختبارًا وقماشته بيضاء والشارة تقول `renderer webgl` — وهو عين
ما يصفه `docs/CI-E2E.md` عن مسار SwiftShader الافتراضي على آلة بلا GPU: **خطأ
تشغيل لا انحدار**. أُعيد بالبيئة الصحيحة، ويُسجَّل هنا لأن أحدًا سيكرّره.

**ومسار المتصفح هنا ليس الحالة المحايدة بل القصوى:** `?stress=12` يوسّع القطعة
إلى 15×10 فتفتح الكاميرا **مثبَّتة عند أرضيتها**، أي عند أقصى مقياس. فما تقيسه
البوابة الوحيدة التي تقرأ بكسلات هو الصورة الجديدة لا القديمة.

---

## 6. الدليل البصري — `docs/hc-p2-s4a-shots/` (81 لقطة + 7 ملفات أرقام، مفهرسة)

الذراعان من الجلسة نفسها والمتصفح نفسه والمنفذ نفسه وصندوق القصّ نفسه ونقطة
الارتكاز نفسها؛ وذراع «قبل» هي شجرة `d41d96c` حرفيًا (`git stash push -- src/render`).

| الملف | ما يُظهره |
|---|---|
| **`plusN-size-plate.png`** | **اللوحة التي تجيب السؤال في صورة**: القصّ نفسه ببكسل الجهاز عند `k=1` (4.8 بكسل CSS) و`k=4` (19.2، المشحون) و`k=5` (24، لو رُفع سقف الغرفة)، ×3 بأقرب جار |
| `plusN-{before,after}-still-phonefit-native-t*ms.png` | القصّ 1:1 ببكسل الجهاز — **ما يراه هاتف فعلًا**: لطخة ذهبية مقابل «+25» يُقرأ |
| `plusN-{before,after}-still-phonefit-full-t*ms.png` | الإطار كاملًا 412×915@2: الرقم بالنسبة إلى الفندق كلّه |
| `plusN-{before,after}-phonefit-*` | الشيء نفسه على عالم يمشي (`?stress=12`) — الحال الحقيقية لا المخبرية |
| `plusN-uncapped-still-phonefit-*` | `k = 5` بسقف غرفة مرفوع عمدًا: **لتبرير السقف لا لشحنه** |
| `plusN-{before,after}-measured.json` | أرقام §4.3 من التشغيلين نفسيهما |

**وما لم يُلتقط** (مكتوب في `INDEX.json` تحت `notTaken`): لقطة لغرفتين
متجاورتين تدفعان معًا — `hct.fx()` يثبّت نقطة الارتكاز على أول شخص داخل الإطار
فلا سبيل منه إلى غرفتين؛ التداخل **محسوب لا مصوَّر** ومسجَّل BL-054 بهذا الوصف.

### 6.1 لماذا `IMPLEMENTED` وليس `VERIFIED`

هذا صندوق: بلا GPU وبلا هاتف، Pixi CanvasRenderer تحت `--disable-3d-apis`. لا
ادعاء بصري هنا أكّدته عين إنسان ولا جهاز. الخطوة تدّعي **الهندسة** — 19.2 بكسل
CSS و38.4 بكسل جهاز و`k = 4` عند 0.40×، وكلّها تُقاس — و**لا تدّعي
المقروئية**. الطريق الوحيد إلى `VERIFIED` قراءة هاتف وفق
`docs/MOBILE-DEVELOPMENT.md`، وقد صار فيها `hct.fxStats().labelScale` و
`labelScaleDrawn` حتى يُقرأ المقياس من الجهاز بدل أن يُخمَّن من لقطة.

---

## 7. ما لم يُصلَح — سُجّل ولم يُلمس

| البند | الحال |
|---|---|
| **BL-053** | البطاقة والعملة تنكمشان بالعلّة نفسها؛ البطاقة **تحت أرضية ART-0 §13**. الإصلاح سطر واحد، والقرار للمالك |
| **BL-054** | تداخل رقمين متجاورين عند المقياس المشحون — **حساب من الثوابت لا لقطة**، ومكتوب أنه كذلك |
| **مدّة الرقم** | `LABEL_LIFE_MS` 500ms بلا تغيير؛ نصف بند §8/3 من قرار S4 **ما زال مفتوحًا** |
| BL-043…BL-052 | كما سُجّلت في S3 وS4، بلا مساس |

---

## 8. أسئلة المالك

1. **هل 19.2 بكسل CSS هو الحجم الذي أردته؟** انظر `plusN-size-plate.png`:
   القصّ نفسه عند 4.8 و19.2 و24. قل رقمًا وتُغيَّر ثابتة واحدة.
2. **وهل تُكبَّر الفقاعة والعملة معه؟** البطاقة اليوم 11.2 × 9.6 بكسل CSS
   وحافّتها 0.8 بكسل جهاز — تحت الأرضية التي أعادت هذه الخطوة الامتثال لها
   للرقم. قل «كبّرها» وتُنفَّذ بالمقياس نفسه (BL-053).
3. **كم يبقى الرقم؟** 500ms كما هي. أتكفي وقد صار الرقم أربعة أضعافه؟
4. **الخطوة التالية** تبقى HC-P2-S5 — المصعد داخل اللوبي (DEC-022). لا تُنفَّذ
   تلقائيًا.

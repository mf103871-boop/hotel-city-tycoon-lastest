# HC-P2-S3 — الهيكل الحي: شخصيات تُوضَع كل إطار بمعدل الشاشة

**التاريخ:** 21-09-2026 (قرار المالك «هيكل حي» 20-09-2026)
**الحالة:** `IMPLEMENTED` (لا `VERIFIED`: لا قماشة على جهاز بمعالج رسوم رُوجعت — قيد DEC-005/DEC-009؛
ما يمكن إثباته على مسار canvas أُثبت، §6.3)
**الفرع:** `claude/wonderful-dirac-i1mqlk` (خط الأساس `2b0113b` — HC-P2-S2 مشحونة وخضراء)
**المرجع الحاكم:** `docs/HOTEL_CITY_MASTER_REFERENCE_AR.md` v1.3
**الاتجاه البصري:** `docs/ART-0_VISUAL_DIRECTION_AR.md` §5 (النِّسَب)، §11 (لغة الأنيميشن — ملاحظة مؤرخة للشخصيات)، §17 (عقد الدمج)؛ `docs/HC-VIS-001-SPEC.md` (:20-:23، :65، :66)
**وثيقة القرار:** `docs/HC-P2-S3-RIG-DECISION.md` (DEC-020، الصف #21؛ الملحق §9 لما غيّرته المراجعة)

> كل رقم في هذا التقرير من تشغيل فعلي مسجَّل بتاريخه (السجلات في `scratchpad/build/` خارج المستودع، أسماؤها
> مذكورة عند كل رقم)، لا من الذاكرة ولا من الخطة. أرقام الصندوق تُسمّى أرقام صندوق، وكل لقطة تُسمّي **طبقتها**
> (`full`/`lite`). كل خانة تحمل «يُملأ بعد التحقق» لم تُملأ بعد ولا يُستنتج منها شيء.

---

## 1. فحص المرجع (قبل أي تعديل)

| ما ينص عليه المرجع | أين | ما فعلته هذه الخطوة |
|---|---|---|
| P2 هدفها ربط كل حالة لعب مهمة بما يراه اللاعب؛ §4.D «الفندق لا يبدو حيًا بما يكفي» | §6 HC-P2 سطر 360-377؛ §4.D سطر 153 | الشخصية هيكل حي يُوضَع كل إطار: خطوة بالمسافة، رأس يتأخر، انضغاط عند التماس، تنفس على شبكة إطارات المقطع |
| ART-0 §11: «8–12 FPS»، «لا يتغير طول الشخصية» (:277)، «لا قفز مستمر» (:280) | ART-0 سطر 259، 273-283 | **تُعدَّل للشخصيات بملاحظة مؤرخة تُلحق** (DEC-020، وثيقة القرار §5)؛ جدول الحالات يبقى مصدر المدد؛ لا سطر حُذف |
| ART-0 §5: رأس 55–65%، طول 58–72% من الداخل | ART-0 سطر 125-127 | تاريخي حيث يتجاوز HC-VIS-001 (ART-0:10)؛ رأس 54% (:22 ✓)، طول 51% من الداخل مسجَّل لـ:23 (BL-043) |
| HC-VIS-001 :22 نسبة الرأس 50–60% | :22 | ✓ 54.0% بلا غطاء لكل التسعة (فحص ذاتي من `pose()`) — يُملأ بعد التحقق: رقم السجل |
| HC-VIS-001 :23 تجربة المقياس «لا يتغير الكود قبل قياس السرير والأبواب ومسار السير معًا» | :23 | **مؤجل — BL-043**: `CHARACTER_ART_SCALE` بقي 0.82؛ لقطات تكبير الغرفة (§6.1) هي القياس؛ الأرقام في وثيقة القرار §4 وسؤال المالك في §9 |
| HC-VIS-001 :65 تثبيت القدم والتوقيت بالمسافة | :65 | **مُحقَّق جزئيًا — انحراف مُعلَن**: الإيقاع بالمسافة ✓، الارتكاز 0px ✓، التثبيت ✗ (انزلاق ≈57px/خطوة عند 128) — فيديو المشية بالمرشحين (§6.1) |
| HC-VIS-001 :66 هوية الوجه والثياب وحجم الرأس | :66 | ✓ قطر الرأس ثابت كل إطار (الرأس يركب الجذع المضغوط) |
| DEC-012: ملف الحركة المصدر الوحيد؛ الورقة مصدر الإطارات | §12 | الشق الأول نافذ (`clipOf`/`progress()`/مدة الرمش من الملف)؛ شق الورقة يُعدَّل بـDEC-020 |
| الصف #19: «الإطارات المرسومة 2–11fps» | `docs/DECISIONS.md` | أُعيد توقيعه صفًا #21؛ «ملف حركة خاص ونقاط تحرك» باقٍ |
| DEC-018 §6: «S3 خبز الهيكل في الأوراق» | `docs/HC-P2-S2-LIGHT-DECISION.md` §6 | حلّ محله الهيكل الحي بقرار المالك؛ §10 مؤرخ هناك |
| DEC-019: بلا Filter ولا mask ولا RenderTexture | §12 | **لم يُعدَّل**: لا `cacheAsTexture`؛ الطبقة `lite` تكميم ونوابض مطفأة فقط (BL-046 لأي اقتراح لاحق) |
| DEC-005/009: لا اعتماد بلا دليل بصري؛ مسار CI لا يقيس الأداء | §12 | السقف `IMPLEMENTED`؛ كل لقطة بطبقتها؛ أرقام الصندوق تُسمّى كذلك |
| DEC-017: الحجم مقاس لا مفروض | §12 | لا ملف جديد في `public/` ولا صورة مُعاد توليدها: `git diff --stat public/ data/` — يُملأ بعد التحقق |
| قاعدة التعديل §1؛ §7 لا توسيع نطاق صامتًا | §1 سطر 32-37؛ §7 سطر 516-533 | القرار قرار المالك النصي جوابًا على سؤال صريح (وثيقة القرار §1)؛ ما فُتح جانبًا: BL-043…046 |

**حالة المدخلات عند البدء:** P2 `IN_PROGRESS` عند HC-P2-S2 (`IMPLEMENTED`، `2b0113b`). P1 `IN_PROGRESS` عند HC-P1-S4
(BL-034 باقٍ). الشجرة نظيفة عند `2b0113b`. النموذج `prototypes/next-render` موجود ويبقى حتى S6. Pixi في `node_modules`
8.21.0 والقفل 8.20.1 (الدليل يُشغَّل على `npm ci`).

---

## 2. الخطة المنفذة

| # | الcommit | ما فيها |
|---|---|---|
| 1 | يُملأ بعد التحقق | الخطوة كلها: الوحدات النقية (`anim/rig.ts`، `anim/cast.ts`، `quality.ts`)، العارض (`characterRig.ts`، `characterView.ts`، `anim/clipPlayer.ts` `progress()`، `scene.ts`، `app.ts`، `index.ts`)، الواجهة (`HotelCanvas.tsx`)، الاختبارات (`tests/unit/rig.test.ts`، مرايا `animations.ts`/`render.ts`/`characters.ts`، `game.spec.ts`)، والحوكمة (وثيقة القرار، DEC-020، الصف #21، ملاحظة ART-0 §11، §11/§13/§17، LIGHT-DECISION §10، هذا التقرير، PROJECT-STATE، README، CI-E2E، MOBILE-DEVELOPMENT) |
| 2 | يُملأ بعد التحقق | ما غيّرته المراجعة العدائية (§4.n وملحق القرار §9) وإعادة الالتقاط |

**شجرة العمل عند إقفال هذا التقرير** (`git diff --stat` و`git status`؛ يُملأ بعد التحقق — لا شيء في `public/` أو `data/`):

```
يُملأ بعد التحقق
```

---

## 3. مطابقة الخطة ↔ المرجع

- **لا رقم توازن تغيّر**: `data/**` لم يُلمس (`git diff --stat data/` — يُملأ بعد التحقق). لا ملف حركة ولا ورقة.
- **لا migration ولا تغيير في الحفظ**: `SCHEMA_VERSION` بقي **20**؛ لا حقل جديد في `GameState`؛ الطبقة والـantialias وحالة
  الهيكل في العارض وحده.
- **النواة والجسر بقيا كما هما**: `src/core/**` و`src/bridge/**` (بما فيه `stress.ts`) بلا تعديل؛ `SceneSnapshot`/`SceneCharacter`
  بلا حقل جديد.
- **ما نقلته الخطة من النموذج وما رفضته** مطابق لوثيقة القرار §2: IK بعظمتين، الخطوة بالمسافة، نابض الرأس (190/17/2.5)،
  الهبوط 2.1 والانضغاط 0.055؛ رُفض `Math.random` (بذرة `mulberry32`)، والانضغاط على الشكل كله (تحت الرقبة فقط)، و`STRIDE 17`
  (→ 128)، والرأس 43% (→ 54%)، والهبوط متوسط-المركز (→ نزولًا فقط)، والتنفس المستمر (→ على الشبكة).
- **صفوف HC-VIS-001** التي تمسّها الخطوة: :22 ✓ 54%؛ :23 مؤجل BL-043 بالأرقام؛ :65 مُحقَّق جزئيًا بانحراف مُعلَن؛ :66 ✓؛
  الباقي في وثيقة القرار §4.
- **النطاق الذي لم يُفتح**: الجسيمات (S4)، المصعد (S5)، parallax وحياة الشارع (S6)، الحوادث الحية (S7)، رافعة الجودة
  وقراءة الهاتف (S8)، تقاعد الأوراق (BL-045)، BL-023، BL-034، BL-039، BL-042.
- **عمل من مرحلة أخرى تسلل؟** يُملأ بعد التحقق (لا شيء مقصود؛ ما تجده المراجعة يُسجَّل هنا وفي §4.n بسببه).
- **انحرافات مسجَّلة عن نص الخطة**: §4.n.

---

## 4. ما تغيّر فعلًا

### 4.0 أين يراه اللاعب

| الموضع / الحالة | ما يراه |
|---|---|
| تكبير الغرفة (2.0×) ونزيل يمشي على الرصيف أو في الممر | خطوة تتبع المسافة (دورة لكل كتلة)، رأس يتأخر عند التوقف والدوران، انضغاط خفيف تحت الرقبة عند التماس — يُملأ بعد التحقق: اسم اللقطة/الفيديو |
| الاستقبال | الموظفة خلف المكتب بقبعة pillbox ولوح، رأسها 54% من شكلها — يُملأ بعد التحقق |
| غرفة نوم | النائم رأسه على الوسادة واللحاف فوق فن السرير، يتنفس على شبكة إطارات المقطع مع انجراف Z — يُملأ بعد التحقق |
| الصالة الرياضية / البار | المدرّب بالدمبل، الساقي بالكوب؛ الأدوات تُقرأ عند 2.0× — يُملأ بعد التحقق |
| واقفون (خمول) | تنفس على إطارات صف `idle` الأربعة، لا زحف تحت-بكسلي؛ رمش 125ms من الملف |
| تقليل الحركة | الوضعية المحايدة مجمَّدة والموضع يسير — `reduced-a.png`/`reduced-b.png` (يُملأ بعد التحقق) |
| مسار CI (`canvas`) والهاتف الضعيف | الطبقة `lite`: الوضع مُكمَّم على إطارات المقطع، النوابض مطفأة؛ نفس الأشكال والألوان |

### 4.1 الرياضيات — `src/render/anim/rig.ts` (جديد، نقي)

يُملأ بعد التحقق (ما شُحن فعلًا: `figureFor`، `ik`، `pose`، `boundsOf`، الثوابت المصدَّرة وقيمها من الملف).

### 4.2 الهوية — `src/render/anim/cast.ts` (جديد، نقي)

يُملأ بعد التحقق (الصفوف التسعة، `PALETTE`، `lookFor`، `shade`؛ فحص التكافؤ في `render.ts` وعدد مقارناته).

### 4.3 الطبقتان — `src/render/quality.ts` (جديد، نقي)

يُملأ بعد التحقق (`tierFor`، `renderFlags`، الحرفان `'0'`/`'1'`).

### 4.4 الرسم — `src/render/characterRig.ts` (جديد)

يُملأ بعد التحقق (عدد الأجزاء الواقفة وأجزاء النوم، عدد الـcontexts المشتركة على مستوى الوحدة، جداول نقاط `poly()`،
`contextFor` و`batchMode`، ما يفعله `apply()` وما لا يفعله — من الفحص الذاتي النصي).

### 4.5 عرض الشخصية — `characterView.ts`، `anim/clipPlayer.ts`

يُملأ بعد التحقق (المفتاحان القذران، مسار الهيكل ومسار الورقة بايتًا ببايت، `progress()`، مدة الرمش من `clipOf(…,'blink')`،
موضع فقاعة الرغبة من أعلى الشكل).

### 4.6 المشهد والتشخيص — `scene.ts`، `app.ts`، `HotelCanvas.tsx`، `index.ts`

يُملأ بعد التحقق (`characterDiagnostics()` بحقول `source:'rig'`/`sx`/`sy`/`sheetReady`، `showCastSheet`، `rigStats`،
`RendererOptions.antialias`، `window.hct.castSheet/castSheetOff/rigStats`).

### 4.7 الاختبارات

يُملأ بعد التحقق (عدد حالات `rig.test.ts`، الفحوص الجديدة في `animations.ts`/`render.ts`/`characters.ts` بأسمائها، تعديل
«characters are drawn» وعتبتا القصّ وقيمهما المطبوعتان).

### 4.8 ما لم يُمسّ

`src/core/**`، `src/bridge/**`، الحفظ (`SCHEMA_VERSION` 20)، `roomView.ts`، `decorView.ts`، `lightLayer.ts`، `backdrop.ts`،
`anim/sheet.ts`، `anim/motion.ts`، `anim/scheduler.ts`، `assets.ts`، `data/**`، `public/**`، ملفات الحركة والأوراق الـ18،
`gen_chars.py`، صف `shipping.ts`، قوائم السماح، `prototypes/`.

### 4.9 ما غيّرته المراجعة العدائية

| # | الوجدان | ما فُعل |
|---|---|---|
| يُملأ بعد التحقق | | |

### 4.10 انحرافات مسجَّلة عن نص الخطة (كل مرحلة، لا انحراف صامت)

كل بند هنا قُرئ من الملف عند الإقفال لا من مذكرات المراحل؛ الخطة تبقى الحاكمة، وما خالفها مسجَّل بسببه:

| # | نص الخطة | ما شُحن | لماذا | أين |
|---|---|---|---|---|
| 1 | وثيقة القرار §1 صف 3: «السؤال كما وُجّه إلى المالك حرفيًا» | مضمون السؤال وخياراه كما وُجّها والجواب «هيكل حي» حرفيًا؛ النص الحرفي للسؤال يُلصق عند الإقفال إن اختلف لفظًا | لم يكن نص السؤال الحرفي في متناول كاتب وثائق هذه الخطوة (لا في المستودع ولا في مخرجات التخطيط)؛ الجواب حرفي | `docs/HC-P2-S3-RIG-DECISION.md` §1 |
| يُملأ بعد التحقق | | | | |

---

## 5. البوابات التي شُغّلت

| البوابة | قبل (`2b0113b`) | بعد (شجرة العمل بعد المراجعة) | ما تثبته |
|---|---|---|---|
| `npm run verify` — vitest | 94 اختبارًا (6 ملفات) | يُملأ بعد التحقق | + `tests/unit/rig.test.ts` (يُملأ بعد التحقق: عدد الحالات) |
| `npm run verify` — selftest | 810 علامة ✓ في مخرجات `verify` كاملة (31 وحدة؛ 782 بطريقة عدّ S2) | يُملأ بعد التحقق | + `animations.ts` ×10، `render.ts` ×4، `characters.ts` ×1 (يُملأ بعد التحقق: الأعداد الفعلية) |
| `npm run lint` | 0 خطأ / 4 تحذيرات no-console سابقة | يُملأ بعد التحقق | لا خطأ جديد |
| `npm run typecheck` | ✓ | يُملأ بعد التحقق | strict + `exactOptionalPropertyTypes` |
| `npm run build` — الميزانيات (مقاسة، DEC-017) | js 348KB gzip، الأصول 361/361 | يُملأ بعد التحقق | الأصول 361/361؛ الأحجام مطبوعة لا مفروضة |
| `check:cheats` | ✓ | يُملأ بعد التحقق | لا `VITE_E2E` ولا `__hct` في `dist` |
| `git diff --stat public/ data/` | فارغ | يُملأ بعد التحقق | لا ملف مُعاد توليده |
| E2E مسار DEC-009 (`--disable-3d-apis`، بلا التقاط، Chromium `/opt/pw-browsers/chromium`، Pixi 8.20.1 من `npm ci`) | 76 ناجحًا / 0 / 12 متخطاة (S2) | يُملأ بعد التحقق | «characters are drawn» يؤكد `source === 'rig'` و`sheetReady` والقصّ؛ مجموعة التخطي = خط الأساس |
| مدد الاختبارات الثلاثة ذات النوافذ (مقرر القائمة) | «characters are drawn» 9.0ث / 8.9ث؛ الوسيط 1.9ث (S2) | يُملأ بعد التحقق | حدود `bootFresh` (20ث/30ث/2.5ث) لم تُمسّ |

فحوص التكافؤ التي يجب أن تبقى بايتًا ببايت — كلها تمر بعد كل تعديل: تكافؤ اللوحة (`render.ts` + `skyLow`)، تكافؤ الليل،
`DESIRE`، تربية `DecorView`، حلقة الشريط الأمامي، سطر `playOnce` على مسار الورقة، `clipOf(`/`assetGeneration()`/`framesFor(`،
مستمعو DOM؛ شُغّلت `render`/`assets`/`regressions`/`characters`/`animations`/`accessibility`/`shipping`/`data-coverage`
منفردةً بعد كل تعديل (يُملأ بعد التحقق: الأعداد) ثم السلسلة كاملة.

**كيف يُعدّ الفحص الذاتي:** كما في تقرير S2 §5 — مجموع أسطر «N checks passed» للوحدات الـ31، أو عدّ كل علامة ✓ في مخرجات
`verify` كاملة (810 على خط الأساس)؛ الطريقتان تُذكران معًا.

---

## 6. الدليل

### 6.1 لقطات مسار canvas — `docs/hc-p2-s3-shots/` (Chromium `--disable-3d-apis`، خادم Vite بـ`VITE_E2E=1`، Pixi 8.20.1، سكربتات `scratchpad/build/s3-*.mjs`)

كل صف يُسمّي **طبقته**. الإزاحة الزمنية في الصندوق 0 (UTC)؛ `epoch` كما في تقرير S2. كل لقطة: سطر الإقلاع `renderer: canvas`،
الشارة بعد `resetPerf()` وثانيتين ونصف، `stats()` و`characters()` مقروءة من الصفحة.

| الملف | الطبقة | الرابط المستعمل | ما يجب أن يظهر | النتيجة (مقروءة من اللقطة والأعداد) |
|---|---|---|---|---|
| `room-reception-full.png` (412×915 @2x، تكبير 2.0) | `full` | `?stress=12&epoch=<ظهر>&debug=1&lite=0` | الاستقبال: الموظفة خلف المكتب، نزيل بحقيبة؛ رأس 54% | يُملأ بعد التحقق: نسبة الرأس وطول الشكل من البكسل، عرض الباب |
| `room-reception-lite.png` | `lite` | الرابط نفسه مع `&lite=1` | ما يرسمه CI: الأشكال نفسها مُكمَّمة | يُملأ بعد التحقق |
| `room-bedroom-full.png` | `full` | … | نائم: الرأس على الوسادة، اللحاف فوق فن السرير | يُملأ بعد التحقق: قياس السرير لـBL-043 |
| `room-gym-full.png` | `full` | … | المدرّب بالدمبل | يُملأ بعد التحقق |
| `room-bar-full.png` | `full` | … | الساقي بالكوب | يُملأ بعد التحقق |
| `desktop-*.png` (1280×720) | `full`/`lite` | … | الغرف نفسها بعرض سطح المكتب | يُملأ بعد التحقق |
| `walk-full.webm` (1280×720، 8ث) | `full` عبر `?lite=0` | تكبير 2.0 على الرصيف ونزيل يصل | الإيقاع بالمسافة، تأخر الرأس، الانضغاط؛ fps المسار البرمجي مسمّى | يُملأ بعد التحقق: fps الفيديو، الانزلاق المقيس |
| `walk-lite.webm` (4ث) | `lite` | `?lite=1` | الشكل المُكمَّم | يُملأ بعد التحقق |
| `walk-stride64.webm` | `full`، `STRIDE_PX 64` (علم بناء مؤقت خارج المستودع) | … | المرشح الثاني لـ:65 | يُملأ بعد التحقق |
| `rig-vs-sheet.png` | لوحة ثابتة | `hct.castSheet({clip:'walk', phase:0.25, scale:2})` بجوار خلايا `docs/art-preview/cast.png` | فرق الرأس 54%/47% | يُملأ بعد التحقق: النِّسَب المقيسة |
| `cast-sheet.png` (1280×720 و2560×1440) | لوحة ثابتة | `hct.castSheet({ scale: 2 })` | 9 صفوف × 10 أوضاع | يُملأ بعد التحقق — صورة ART CHECK |
| `reduced-a.png` / `reduced-b.png` | `full` + `reducedMotion: 'reduce'` | إطاران بفاصل 700ms | وضع متطابق، `sx` مختلف | يُملأ بعد التحقق: فرق القصّ و`sx` |
| `webgl-attempt.md` | SwiftShader | المسار الافتراضي | **ملاحظة بيئة** (BL-020)؛ قراءة داخلية لقصّ هيكل إن رُسم قبل التجمد | يُملأ بعد التحقق |

### 6.2 أرقام الصندوق — تُسمّى أرقام صندوق (**ليست** قياس هاتف؛ لا تُقارن بحد p5 ≥ 55)

`window.hct.perf()` و`window.hct.rigStats()` عند `?stress=60&warm=900&debug=1` بعد `resetPerf()` ثم 10ث (~600 إطار)؛ السجلات
`scratchpad/build/s3-perf-*.json` (يُملأ بعد التحقق: الأسماء):

| المسار | الطبقة | العرض | fps متوسط | p5 | إطارات متأخرة | ذاكرة | ناس مرسومون / الكل | `rigStats().parts` | `rigStats().rebuilds` (إطارات/10ث) |
|---|---|---|---|---|---|---|---|---|---|
| Canvas2D، بلا GPU | `lite` (ما يرسمه CI) | 412×915@2x | يُملأ بعد التحقق | | | | | | |
| Canvas2D، بلا GPU | `full` (`?lite=0`) | 412×915@2x | يُملأ بعد التحقق | | | | | | |
| Canvas2D، بلا GPU | `lite` / `full` | 1280×720 | يُملأ بعد التحقق | | | | | | |
| Canvas2D، بلا GPU | خط أساس الأوراق (S2) مُعاد تشغيله في اليوم نفسه | 412×915@2x | يُملأ بعد التحقق (S2 سجّل 58.3 / 59.5 / 2.9% نهارًا) | | | | 35 / 61 (S2) | — | |
| SwiftShader WebGL، CPU فقط (اللقطة بيضاء BL-020؛ القراءة عند t ≈ 25ث) | `full` | 412×915@2x | يُملأ بعد التحقق | | | | | | |
| SwiftShader WebGL، CPU فقط | `lite` | 412×915@2x | يُملأ بعد التحقق | | | | | | |

`isBatchable` لكل context على مسار WebGL: يُملأ بعد التحقق. مدد اختبارات E2E ذات النوافذ: يُملأ بعد التحقق. أرقام القصّ التي
يطبعها اختبار «characters are drawn» (الألوان المتميزة، نسبة الحبر): يُملأ بعد التحقق.

القراءة: يُملأ بعد التحقق (ما تقوله الأرقام عن كلفة الطبقتين على Canvas2D وعن كلفة JavaScript على WebGL البرمجي؛ أي رقم منها
على هاتف حقيقي مجهول — §6.3).

### 6.3 لماذا `IMPLEMENTED` وليس `VERIFIED`

ما تعد به هذه الخطوة أن يراه اللاعب — شخصية تمشي بخطوة تتبع المسافة ورأس يتأخر وانضغاط عند التماس — يُرى في لقطات
وفيديو مسار canvas في §6.1 بطبقة `full` عبر `?lite=0`. لكن مسار canvas يثبت أن Pixi يرسم الهيكل على Canvas2D **في صندوق بلا
معالج رسوم** بحواف ناعمة؛ لا يثبت شكل الحواف المتعرجة على WebGL/WebGPU (`antialias:false`) ولا وميضها، ولا أن الصورة على
شاشة هاتف حقيقي مقبولة عند عين المالك، ولا أن p5 ≥ 55fps عند 60 غرفة ونحو 60 شخصًا بالهيكل الحي. المحاولة على WebGL البرمجي
(SwiftShader) تعطي قراءة CPU داخل نافذة التجمد (BL-020) ولا تُعرض دليلًا. الطريق الوحيد إلى `VERIFIED` هو إجراء
`docs/MOBILE-DEVELOPMENT.md` على هاتف مع طراز الجهاز ومعرّف البناء، بالقراءات الثلاث (`?lite=0/1` و`?aa=1`) و`hct.rigStats()`
(DEC-005/DEC-009).

---

## 7. ما لم يُنفذ — سُجّل ولم يُلمس (§7)

| المعرّف | البند | الحالة |
|---|---|---|
| BL-043 | تجربة `CHARACTER_ART_SCALE` 0.85–0.95 بعد قياس S3 للسرير والأبواب والمسار معًا (HC-VIS-001 :23) — الأرقام في وثيقة القرار §4 | جديد، متوسطة — ينتظر المالك (§9) |
| BL-044 | قياس `antialias`/MSAA على الجهاز عبر `?aa=1`؛ الحواف المتعرجة ووميض الحواف عند الخمول | جديد، متوسطة — قراءة هاتف |
| BL-045 | تقاعد الأوراق الـ18 وصور مصغرة من الهيكل | جديد، منخفضة — توقيع مستقل (S9 أو ضمن S8) |
| BL-046 | خزن الهيكل نسيجًا (`cacheAsTexture`) على مسار canvas — مرفوض في S3 (DEC-019) | جديد، منخفضة — لا يُقترح إلا باستثناء مؤرخ |
| BL-036 | المصعد داخل اللوبي | مجدول HC-P2-S5 |
| BL-039 / BL-040 / BL-042 | كما سُجّلت في S2 | `BACKLOG` |

خارج النطاق أيضًا: الجسيمات (S4)، parallax وحياة الشارع (S6)، الحوادث الحية (S7)، رافعة الجودة (S8)، BL-023، BL-034،
ART-REFRESH (HC-VIS-001 بوابة 1).

---

## 8. مطابقة التنفيذ ↔ المرجع

| بند بوابة P2 / شرط قبول | الحالة | الدليل |
|---|---|---|
| الشخصيات هيكل حي يُوضَع كل إطار؛ الخطوة بالمسافة (128px = دورة)؛ الواقف طوره مجمَّد | يُملأ بعد التحقق | `rig.test.ts`، `animations.ts` «distance-driven stride»؛ `walk-full.webm` |
| رأس 54% (:22)؛ أعلى نقطة ≤ 70 وأعرض نصف مدى ≤ 24 لكل عضو × مقطع × طور | يُملأ بعد التحقق | `animations.ts` (الجدول المطبوع لكل عضو)، `rig.test.ts` |
| برهان المدى: الورك الاسمي ≤ الرجل − 0.001 وأقصى مسافة على الدورة ≤ الاسمي | يُملأ بعد التحقق | `rig.test.ts` |
| الرأس يركب الرقبة المضغوطة (≤ 1e-6)؛ قطر الرأس ثابت (:66) | يُملأ بعد التحقق | `rig.test.ts` |
| تنفس الوقوف/الجلوس/النوم على شبكة إطارات المقطع في الطبقتين | يُملأ بعد التحقق | `rig.test.ts` |
| تقليل الحركة: φ = 0، t = 0 حرفيًا، لا قراءة لحالة الهيكل، بذرتان → وضع متطابق؛ الموضع يسير | يُملأ بعد التحقق | `rig.test.ts`، `animations.ts`، `reduced-a/b.png` |
| هوية الطاقم مطابقة لـ`characters.py`/`hcstyle.P` حقلًا حقلًا ولونًا لونًا؛ `shade` نحو (10,20,44) | يُملأ بعد التحقق | `render.ts` ×4 |
| `apply()` مواضع/دوران/مقياس/ألفا فقط؛ `new GraphicsContext(` مرة واحدة؛ `batchMode 'batch'`؛ لا `cacheAsTexture`/RenderTexture | يُملأ بعد التحقق | `animations.ts` (فحص نصي) |
| مسار الورقة بايتًا ببايت (`playOnce` line)، `clipOf`/`assetGeneration`/`framesFor`/`lookFor` | يُملأ بعد التحقق | `animations.ts`، `assets.ts` |
| كل شخص مرسوم بالهيكل والأوراق وصلت؛ الهيكل يرسم بكسلات على مسار CI | يُملأ بعد التحقق | `game.spec.ts` «characters are drawn» (`source === 'rig'`، `sheetReady`، القصّ) |
| الأوراق الـ18 بلا تغيير؛ فحوصها الأربعون تمر | يُملأ بعد التحقق | `assets.ts`، `animations.ts:196-360`، `characters.ts` |
| لا `Math.random` ولا DOM عند الاستيراد في `rig`/`cast`/`quality` | يُملأ بعد التحقق | `animations.ts` فحص النقاء |
| لا تغيير في النواة أو الجسر أو الحفظ؛ لا ملف في `public/` (DEC-017) | يُملأ بعد التحقق | `git diff --stat` |
| **الأداء p5 ≥ 55fps عند 60 غرفة ونحو 60 شخصًا بالهيكل الحي** | ⚠️ | يحتاج هاتفًا (§6.3)؛ أرقام الصندوق في §6.2 لا تُقارن به |
| **مراجعة بصرية على جهاز حقيقي** (التعرّج ووميض الحواف عند `?aa=0/1`) | ⚠️ | DEC-005/DEC-009: على المالك بعد النشر (BL-044) |

### ART CHECK

- **هل رُسم فن جديد بيد Fable؟** لا ملف. الهيكل نقل حرفي لأشكال `hcstyle.py` بأرقامها؛ لا placeholder، ولا أصل من خارج
  المستودع، ولا صورة مُعاد توليدها (`git diff --stat public/` — يُملأ بعد التحقق).
- **المرجع المعتمد مقابل لوحة الطاقم من الهيكل (`cast-sheet.png`) ولقطات تكبير الغرفة:** يُملأ بعد التحقق مما يُرى — رؤوس كبيرة،
  حدود سوداء، باستيل، أدوات تُقرأ، الرأس على الوسادة؛ ما يطابق وما لا يطابق بعد (S4–S6، BL-045).
- **HC-VIS-001 :23 (المقياس):** الأرقام المقيسة من اللقطات (طول الشكل، عرض الباب، السرير) — يُملأ بعد التحقق؛ القرار للمالك.
- **:65 (الانزلاق):** المقيس من `walk-full.webm` و`walk-stride64.webm` — يُملأ بعد التحقق؛ القرار للمالك.
- **هل احتُرمت لغة ART-0 §11؟** بملاحظة مؤرخة لا بصمت (وثيقة القرار §5)؛ السعات المقيدة من الفحص الذاتي — يُملأ بعد التحقق.

---

## 9. سطور نهاية الجلسة (§16)

1. **المرحلة والخطوة:** HC-P2 «جعل الفندق حيًا بصريًا» — الخطوة HC-P2-S3 «الهيكل الحي». P1 ما زالت `IN_PROGRESS` (BL-034).
2. **ما أصبح `VERIFIED`:** لا شيء جديد. ما يمكن إثباته في صندوق بلا GPU أُثبت — الرياضيات والحدود والتكافؤ ورسم Pixi على
   Canvas2D ولقطات المسار بطبقتيه — لكن أيًّا منها ليس عين المالك ولا قياس هاتف.
3. **ما بقي `IMPLEMENTED` فقط:** HC-P2-S3 كلها. `npm run verify`: يُملأ بعد التحقق؛ `npm run build`: يُملأ بعد التحقق؛ E2E مسار
   DEC-009: يُملأ بعد التحقق.
4. **ما ينتظر المالك:**
   - **مقياس العرض (BL-043، :23):** بعد رؤية لقطات تكبير الغرفة (شكل 49.2px = 51% من الداخل، باب 40.1/44px) — هل تجرّب 0.85
     (51.0px) أو 0.95 (57.0px) أم يبقى 0.82؟
   - **الانزلاق (:65):** فيديو المشية يعرض `STRIDE_PX` 128 (انزلاق ≈57px/خطوة، إيقاع الملف) و64 (≈25px، إيقاع مضاعف) — أيهما تعتمد؟
   - **الحواف على الهاتف (BL-044):** عند قراءة الهاتف قارن `?aa=0` و`?aa=1` وقل أيهما تعتمد وهل تلاحظ وميضًا عند المشي أو الخمول.
   - **مصير الأوراق (BL-045):** تقاعد الأوراق وصور مصغرة من الهيكل خطوة S9 مستقلة أم تُضم إلى S8؟
   - **ترتيب السلّم بعد S3:** S4 الجسيمات → S5 المصعد → S6 العمق وحذف النموذج → S7 الحوادث والطقس → S8 رافعة الجودة وقياس
     الهاتف — تأكيد صريح (سؤال تقرير S2 ما زال مفتوحًا).
   - **قراءة الهاتف:** `docs/MOBILE-DEVELOPMENT.md` مع `?stress=60&warm=900&debug=1` ثم `&lite=1` ثم `&aa=1` مع
     `window.hct.perf()` و`window.hct.rigStats()` — من يجريها ومتى؛ هي الطريق الوحيد من `IMPLEMENTED` إلى `VERIFIED`.
5. **الخطوة التالية الوحيدة:** HC-P2-S4 — الجسيمات والدخل العائم وفقاعات one-shot (نطاق «S2» القديم)، صالحة لـCanvas2D
   (DEC-019). لا تُنفَّذ تلقائيًا، وتبدأ بوثيقة قرار (DEC-021).

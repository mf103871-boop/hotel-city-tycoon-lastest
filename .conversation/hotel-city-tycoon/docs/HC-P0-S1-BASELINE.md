# HC-P0-S1 — خط الأساس القابل للتكرار

**تاريخ القياس:** 2026-09-01  
**المنطقة الزمنية:** Asia/Amman  
**الحالة:** `BLOCKED`  
**النطاق:** قياس خط الأساس فقط؛ لا إصلاحات ميزات، ولا تغيير اقتصاد، ولا انتقال إلى خطوة أخرى.

## فحص المرجع

- **إصدار المرجع:** `HOTEL_CITY_MASTER_REFERENCE_AR.md` الإصدار 1.2، بتاريخ 31 أغسطس 2026.
- **المرجع البصري:** `ART-0_VISUAL_DIRECTION_AR.md` الإصدار 1.0، حالته `STYLE_DIRECTION_APPROVED`، مع `VISUAL_NORTH_STAR.jpeg` كمرجع أسلوب وتكوين فقط.
- **المرحلة الحالية:** HC-P0 — تثبيت خط الأساس.
- **الخطوة الحالية:** HC-P0-S1 — إنشاء خط أساس قابل للتكرار في الكلاود.
- **الهدف الواحد لهذه الخطة:** إثبات نقطة البداية الفعلية في بيئة الكلاود وتسجيلها، من دون إصلاح ميزات أو تغيير اقتصاد.
- **المشكلة المثبتة التي تعالجها:** لا يوجد إثبات متكرر لتثبيت المشروع وتشغيل مجموعات التحقق وPlaywright في بيئة الكلاود.
- **الاعتماديات المكتملة:** المرجعان النصيان والصورة متوفرة الآن في `docs/`؛ `package.json` و`package-lock.json` متوفران؛ لا يوجد `pnpm-lock.yaml`.
- **ما يدخل في النطاق:** اختيار npm من lockfile الموجود، تنصيب نظيف، أوامر التحقق المحددة، تثبيت Chromium، محاولة E2E، وتسجيل البيئة والأدلة.
- **ما لا يدخل في النطاق:** إصلاح الميزات، إصلاح الاقتصاد أو المستويات، محرر الديكور، إنتاج أي أصل بصري، أو إنشاء sprite/animation.
- **شروط القبول:** نتائج حرفية قابلة لإعادة التنفيذ، Playwright يبدأ وينفذ الاختبارات أو يسجل `BLOCKED` بسبب محدد، ولقطات مرجعية فقط إذا أصبحت بيئة المتصفح متاحة.
- **الاختبارات والأدلة المطلوبة:** `validate:data`، `typecheck`، `lint`، `test:logic`، `test:selftest`، `build`، `npx playwright install chromium`، `test:e2e`، وسجل البيئة.
- **التعارضات أو المعلومات الناقصة:** لا يوجد تعارض مرجعي بعد وصول حزمة المرجع. توجد علة بيئية تمنع Chromium: أولًا `libglib-2.0.so.0` ثم، بعد تثبيت الحزم الموثقة، `libgbm.so.1`.

## خطة HC-P0-S1 — إنشاء خط أساس قابل للتكرار في الكلاود

### الهدف

- **النتيجة التي يراها اللاعب:** لا توجد نتيجة بصرية معتمدة في هذه الخطوة؛ المطلوب إثبات أن المشروع يمكن تثبيته وبناؤه وتشغيل اختباراته في الكلاود.
- **خط الأساس الحالي ودليله:** حالة المرجع كانت `READY`، والتقارير السابقة ادعت نجاح الفحوص الرأسية وعدم إثبات E2E في بيئة متصفح سليمة. هذا التقرير يستبدل الادعاء بنتائج تشغيل فعلية لهذه البيئة.

### النطاق

- **الملفات المتوقع فحصها:** `package.json`، `package-lock.json`، `.replit`، `PROJECT-STATE.md`، `docs/*`، وملفات الاختبارات.
- **الملفات المتوقع تعديلها:** إضافة هذا التقرير ونسخ حزمة المرجع إلى `docs/`؛ لا تعديل على `src/` أو `data/` أو lockfile.
- **التبعيات:** Node.js وnpm، lockfile عام، وحزم Chromium النظامية.
- **ما لن يتغير:** منطق اللعب، الاقتصاد، المحتوى، الأصول، الحفظ، وواجهات اللعبة.

### خطوات التنفيذ

1. فحص مدير الحزم والـlockfile.
   - **الأنظمة/الملفات:** `package.json`، `package-lock.json`.
   - **النتيجة المتوقعة:** اعتماد npm و`npm ci` من lockfile الموجود من دون إعادة توليد.
   - **طريقة التحقق:** فحص وجود lockfile ثم نتيجة `npm ci`.
   - **شرط التوقف:** وجود lockfiles متنافسة أو فشل التثبيت.
2. تنفيذ أوامر التحقق المحددة كل واحد على حدة.
   - **الأنظمة/الملفات:** validators، TypeScript، ESLint، Vitest، self-tests، Vite build.
   - **النتيجة المتوقعة:** تسجيل رمز الخروج والمدة وعدد الاختبارات/الفحوص.
   - **طريقة التحقق:** المخرجات الفعلية لكل أمر.
   - **شرط التوقف:** لا تُخلط إصلاحات مع القياس.
3. تثبيت Chromium وتشغيل Playwright.
   - **الأنظمة/الملفات:** Playwright وبيئة النظام.
   - **النتيجة المتوقعة:** يبدأ المتصفح وتنفذ الاختبارات.
   - **طريقة التحقق:** عدد الاختبارات المنفذة ورسالة إطلاق المتصفح.
   - **شرط التوقف:** إذا لم يبدأ المتصفح أو كان العدد صفرًا، الحالة `BLOCKED`.
4. التقاط اللقطات المرجعية إذا أصبحت بيئة المتصفح متاحة.
   - **الأنظمة/الملفات:** الشاشة الرئيسية، البناء، متجر الديكور، الإدارة، وعرض الهاتف.
   - **النتيجة المتوقعة:** لقطات فعلية مرتبطة بهذا القياس.
   - **طريقة التحقق:** ملفات صور ناتجة من جلسة المتصفح.
   - **شرط التوقف:** لا لقطات وهمية أو لقطات من المرجع بدل اللعبة.

### شروط القبول

- مدير حزم واحد وlockfile واحد قابل لإعادة التنفيذ.
- كل أمر أساسي منفصل مسجل برمز الخروج والمدة.
- لا يُكتب `PASS` لـPlaywright إذا لم يبدأ المتصفح أو كان عدد الاختبارات المنفذة صفرًا.
- توجد لقطات فعلية فقط إن توفرت بيئة متصفح سليمة.
- لا توجد تعديلات وظيفية أو تغييرات اقتصاد مختلطة مع القياس.

### المخاطر وخطة الرجوع

- **خطر الحفظ أو migration:** لا يوجد؛ لم يتغير كود الحفظ أو schema.
- **خطر الأداء:** لم تُجرَ قياسات أداء داخل المتصفح لأن Chromium لم يبدأ.
- **طريقة التراجع بلا فقد بيانات:** حذف التقرير وملفات المرجع المضافة فقط؛ `src/` و`data/` وlockfile لم تُمس.

## مطابقة الخطة مع المرجع

- **المرحلة المطابقة:** HC-P0.
- **المشكلة المرجعية التي تعالجها:** إثبات خط أساس قابل للتكرار في الكلاود قبل أي تطوير.
- **البنود التي لن تلمسها الخطة:** الاقتصاد، الديكور المرئي، المحتوى، الاجتماعيات، وإصلاحات P0 اللاحقة.
- **هل تسللت أعمال من مرحلة أخرى؟** لا.
- **هل كل الخطوات قابلة للتحقق؟** نعم، باستثناء اللقطات التي تعتمد على إتاحة Chromium؛ لها حالة توقف صريحة.
- **هل توجد افتراضات غير مثبتة؟** نعم: توافر مكتبات Chromium النظامية في البيئة الحالية.
- **هل تحقق الخطة شرط خروج الخطوة؟** لا قبل التنفيذ؛ القرار قبل التنفيذ: صالحة للقياس فقط.
- **القرار:** صالحة للتنفيذ.

## تقرير التنفيذ

### البيئة ومدير الحزم

```text
node v24.13.0
npm 11.6.2
Playwright 1.62.1
Linux repl 6.18.48 #Replit-Linux SMP Fri Aug 28 06:22:54 UTC 2026 x86_64 GNU/Linux
package-lock.json lockfileVersion=3
package name=hotel-city-tycoon
package version=0.1.0
pnpm-lock.yaml: غير موجود
```

تم اعتماد **npm**. نُفّذ `npm ci` بعد حذف `node_modules`، ولم يُعاد توليد `package-lock.json`.

```text
command=npm ci
exit=0
duration=10s
result=added 241 packages; audited 242 packages; found 0 vulnerabilities
```

### نتائج الأوامر المنفصلة

#### 1. `npm run validate:data`

```text
exit=0
duration_ms=1157
```

النتيجة الحرفية المهمة:

```text
✓ 16 files · 23 rooms · 77 decor · 7 roles · 2 guest types · 52 levels
✓ all cross-references resolve
✓ 241 asset entries · 241 drawn · 63/63 required present
0 error(s), 0 warning(s)
0 file(s) failed schema validation
```

#### 2. `npm run typecheck`

```text
exit=0
duration_ms=12518
```

#### 3. `npm run lint`

```text
exit=0
duration_ms=7722
✖ 4 problems (0 errors, 4 warnings)
```

التحذيرات الأربعة كلها `no-console` موجودة في:

```text
src/main.tsx:63
src/main.tsx:64
src/ui/HotelCanvas.tsx:175
src/ui/HotelCanvas.tsx:179
```

#### 4. `npm run test:logic`

```text
exit=0
duration_ms=6630
Test Files  3 passed (3)
Tests       33 passed (33)
```

#### 5. `npm run test:selftest`

```text
exit=0
duration_ms=75219
aggregate=628 checks passed across 28 modules
```

المخرجات التي ظهرت أثناء التنفيذ تضمنت:

```text
29 checks passed
...
18 checks passed
```

والتجميع الحالي المعلن في `PROJECT-STATE.md` و`docs/RECORDS-6A-REPORT.md` هو `628/628` عبر 28 وحدة. لم يُعد تشغيل الأمر لمجرد تجاوز حد عرض المخرجات؛ رمز الخروج صفر، ولم يظهر فشل في التنفيذ.

#### 6. `npm run build`

```text
exit=0
duration_ms=25368
vite v6.4.3 building for production...
✓ 855 modules transformed.
✓ built in 11.45s
every budget met
✓ assets shipped    241 /   241
✓ the bundle carries no test handle
```

تفصيل ميزانية البناء:

```text
js (gzip)         321KB /   350KB
css (gzip)          6KB /    60KB
initial art       802KB /  3072KB
audio             118KB /   400KB
art and audio    2858KB /  8192KB
```

#### 7. `npx playwright install chromium`

```text
exit=0
duration_ms=45883
result=Chromium 151.0.7922.34 and its Playwright runtime files downloaded
```

#### 8. `npm run test:e2e` — المحاولة الأولى

```text
exit=1
duration_ms=100119
planned=82 tests using 1 worker
executed=0 application tests
result=82 failed at browser launch
error=libglib-2.0.so.0: cannot open shared object file: No such file or directory
```

هذا ليس `PASS`: المتصفح لم يبدأ، والعدد الفعلي لاختبارات التطبيق كان صفرًا.

#### 9. تثبيت متطلبات النظام الموثقة

استُخدم مدير تبعيات Replit للنظام بالحزمة المطلوبة نفسها:

```text
glib
nss
atk
at-spi2-atk
gtk3
alsa-lib
libdrm
mesa
result=success
```

تدار إعدادات النظام في هذه البيئة عبر أداة Replit، لذلك لم يُكتب `replit.nix` يدويًا. لا يوجد تعديل على كود اللعبة أو بياناتها أو lockfile.

#### 10. `npx playwright install chromium` — بعد متطلبات النظام

```text
exit=0
duration_ms=3002
```

#### 11. `npm run test:e2e` — المحاولة الثانية

```text
exit=1
duration_ms=63418
planned=82 tests using 1 worker
executed=0 application tests
result=82 failed at browser launch
error=libgbm.so.1: cannot open shared object file: No such file or directory
```

بعد تثبيت الحزم الموثقة، انتقل الخطأ من `libglib-2.0.so.0` إلى `libgbm.so.1`. لذلك لا يجوز تسجيل E2E كناجح، ولا يجوز نسب الفشل إلى كود اللعبة.

### التحقق البصري واللقطات

لم تُلتقط لقطات للشاشة الرئيسية أو البناء أو متجر الديكور أو الإدارة أو عرض الهاتف. السبب المحدد: Chromium لم يبدأ في كلتا المحاولتين، ولم تُفتح صفحة اللعبة أصلًا. لم تُستخدم صورة `VISUAL_NORTH_STAR.jpeg` كلقطة للعبة، ولم تُولّد أي صور أو رسوم أو sprite sheets أو animation.

### أثر التغيير على الحفظ

لا تغيير. لم تُعدّل ملفات `src/` أو `data/` أو schema أو migration أو منطق الحفظ.

### أثر التغيير على الأداء

لا قياس أداء داخل اللعبة؛ تشغيل المتصفح محجوب قبل بدء الصفحة. `npm run build` اجتاز ميزانيات الحزمة دون تغيير وظيفي.

### الانحرافات عن الخطة وأسبابها

- حزمة المرجع وصلت بعد بدء القياس الأول؛ أضيفت ملفات المرجع إلى `docs/` قبل تشغيل القياسات المكتملة.
- لم تُنشأ لقطات لأن شرط توفر المتصفح لم يتحقق.
- لم يُكتب `replit.nix` يدويًا لأن إعدادات النظام محكومة بأداة Replit؛ ثُبّتت الحزم الثمانية عبر مدير النظام نفسه.

### مشكلات جديدة أضيفت للمؤجل

لا تُنفذ الآن:

| المعرّف | المشكلة | الحالة |
|---|---|---|
| HC-P0-S1-BL-001 | بيئة Chromium تفتقد `libgbm.so.1` حتى بعد تثبيت مجموعة الحزم الموثقة؛ E2E لا يبدأ | `BLOCKED` |
| HC-P0-S1-BL-002 | لا توجد لقطات مرجعية لأن المتصفح لم يبدأ | `DEFERRED` حتى حل BL-001 |

### حقيقة جديدة يجب إضافتها إلى المرجع

في هذه البيئة، تثبيت Chromium نجح، لكن إطلاق Chromium ظل محجوبًا بسبب `libgbm.so.1` بعد تثبيت `glib`, `nss`, `atk`, `at-spi2-atk`, `gtk3`, `alsa-lib`, `libdrm`, و`mesa`. هذه حقيقة بيئية، وليست حكمًا على كود اللعبة.

### الحالة

`BLOCKED`

### الخطوة التالية الوحيدة، من دون تنفيذها

`HC-P0-S2 — إصلاح عوائق التثبيت والاختبارات التي يثبتها HC-P0-S1`.

## مطابقة التنفيذ مع المرجع

- **هل تحقق الهدف الأصلي؟** جزئيًا: تم إثبات التثبيت، البيانات، الأنواع، lint، المنطق، self-tests، والبناء؛ لم يكتمل إثبات المتصفح أو اللقطات.
- **هل اجتازت جميع شروط القبول؟** لا؛ Playwright لم يبدأ في المحاولتين، واللقطات غير متاحة.
- **هل تغير شيء غير مصرح به؟** لا في كود اللعبة أو الاقتصاد أو البيانات. أضيفت ملفات المرجع وتقرير القياس فقط، وثُبتت تبعيات النظام.
- **هل بقي افتراض غير مثبت؟** نعم: أن مجموعة الحزم الموثقة تكفي لإتاحة `libgbm.so.1` في بيئة التشغيل الحالية.
- **هل يمكن الانتقال للخطوة التالية؟** لا تلقائيًا. الحالة الحالية `BLOCKED`، والخطوة الوحيدة التالية هي HC-P0-S2 من دون تنفيذها هنا.

## نهاية الجلسة

1. **المرحلة والخطوة الحالية:** P0 / HC-P0-S1.
2. **ما أصبح `VERIFIED` بالدليل:** npm clean install، data validation، typecheck، lint بلا أخطاء، 33 اختبار منطق، self-test aggregate 628/628، production build والميزانيات.
3. **ما بقي `IMPLEMENTED` فقط:** لا توجد ميزة جديدة نُفذت في هذه الخطوة؛ E2E واللقطات غير مثبتة.
4. **العائق أو القرار المنتظر من المالك:** حل `libgbm.so.1` في بيئة Chromium؛ لا اعتماد لخط الأساس قبل تشغيل الاختبارات فعليًا.
5. **الخطوة التالية الوحيدة دون تنفيذ تلقائي:** HC-P0-S2.
اقرأ docs/HOTEL_CITY_MASTER_REFERENCE_AR.md وdocs/HC-P0-S2B-REPORT.md قبل أي عمل. نفّذ الآن HC-P0-S2C وحدها: عزل الفرق بين probe المباشر الناجح وفشل fixture في Playwright Test على channel chromium، وفتح المجموعة الكاملة واللقطات عند أول نجاح. ابدأ بقسم «فحص المرجع» ثم قالب الخطة ثم «مطابقة الخطة مع المرجع» قبل التنفيذ. ممنوع تعديل src/ أو data/ أو package-lock.json أو package.json أو منطق أي اختبار في tests/e2e. التعديلات الوحيدة المسموحة: حارس واحد جديد محدد أدناه في playwright.config.ts، وملفان جديدان tools/playwright-diag.config.ts وtools/diag-spec/min.spec.ts، وtools/baseline-shots.mjs عند نجاح الإثبات فقط، وحزمة xvfb-run عبر أداة التبعيات إن بلغت مرحلتها فقط. ممنوع توليد أي صور أو رسوم أو sprite sheets أو animation. حقيقة بيئة مثبتة من S2B يجب احترامها: ممنوع إضافة --disable-gpu في أي تشغيل لأنه يكسر التقاط اللقطات.

1) أضف في playwright.config.ts داخل use بعد سطري trace وscreenshot الحاليين هذا الحارس حرفيًا من دون أي تغيير آخر:

...(process.env.PLAYWRIGHT_NO_CAPTURE === '1'
  ? { trace: 'off', screenshot: 'off', video: 'off' }
  : {}),

2) الاختبار T1: شغّل

env -u PLAYWRIGHT_CHROMIUM_PATH PLAYWRIGHT_FULL_CHROMIUM=1 PLAYWRIGHT_DISABLE_DEV_SHM=1 PLAYWRIGHT_NO_CAPTURE=1 npx playwright test --project=desktop -g "boots past the loading screen"

واحفظ الناتج الحرفي في docs/HC-P0-S2C-T1.log. إن نجح فانتقل مباشرة إلى الخطوة 6.

3) إن فشل T1: أعده مرة واحدة بإضافة DEBUG=pw:api,pw:browser* واحفظ السجل كاملًا في docs/HC-P0-S2C-api.log، وحدد في التقرير آخر نداء API بدأ ولم يكتمل قبل التجمد، بالحرف.

4) الاختبار T2 لعزل webServer: شغّل بالخلفية HOST=127.0.0.1 PORT=5000 VITE_E2E=1 npm run dev وانتظر جاهزية http://127.0.0.1:5000، ثم أعد أمر T1 نفسه ليعاد استخدام الخادم القائم، واحفظ docs/HC-P0-S2C-T2.log. إن نجح فانتقل إلى الخطوة 6 مع إبقاء الخادم اليدوي ضمن التركيبة وتسجيل أن webServer المدمج هو العائق.

5) إن فشل T2: أنشئ منصة معزولة — tools/playwright-diag.config.ts بإعداد أدنى: testDir يشير إلى tools/diag-spec، بلا webServer وبلا trace وبلا screenshot، وuse فيه channel: 'chromium' وlaunchOptions args تحوي --disable-dev-shm-usage فقط، وtools/diag-spec/min.spec.ts باختبار واحد يفتح http://127.0.0.1:5000 ويتحقق من ظهور body. مع الخادم اليدوي قائمًا شغّل npx playwright test --config tools/playwright-diag.config.ts واحفظ docs/HC-P0-S2C-T3.log. إن نجح المعزول بينما فشل الرئيسي فتوقف وسجّل BLOCKED مع الفارق الدقيق بين الإعدادين وخيار المالك بنقل E2E إلى GitHub Actions. وإن فشل المعزول أيضًا: ثبّت xvfb-run عبر أداة التبعيات وسجّل الاسم الذي نجح، ثم شغّل

xvfb-run -a env -u PLAYWRIGHT_CHROMIUM_PATH PLAYWRIGHT_FULL_CHROMIUM=1 PLAYWRIGHT_HEADED=1 PLAYWRIGHT_DISABLE_DEV_SHM=1 PLAYWRIGHT_NO_CAPTURE=1 npx playwright test --project=desktop -g "boots past the loading screen"

واحفظ docs/HC-P0-S2C-T4.log. إن نجح فانتقل إلى الخطوة 6 بهذه التركيبة، وإن فشل فتوقف نهائيًا وسجّل BLOCKED بكل السجلات وخيار GitHub Actions قرارًا للمالك.

6) بأول تركيبة نجح بها الإثبات، ومن دون تغيير أي متغير فيها: شغّل npm run test:e2e كاملًا بنفس الصياغة، مسبوقًا بـxvfb-run -a إن كانت التركيبة من مسنّد الخطوة 5، واحفظ docs/HC-P0-S2C-e2e-full.log. سجّل حرفيًا: المخطط 82، المنفذ فعليًا، الناجح والفاشل لكل مشروع من desktop وphone، المدة الكلية، وأسماء كل الاختبارات الفاشلة. ممنوع إصلاح أي فشل تطبيقي؛ أضف كل فشل إلى جدول المؤجل بمعرّف جديد ووصف وخطورة ومرحلة مقترحة.

7) أغلق HC-P0-S1-BL-002: أنشئ tools/baseline-shots.mjs يشغّل الخادم بنفس إعدادات الخطوة 4 ويستخدم playwright بنفس تركيبة النجاح ويلتقط من الجلسة الحية لقطات الشاشة الرئيسية ولوحة البناء ومتجر الديكور ولوحة الإدارة وعرض هاتف بمقاس Pixel 7، ويحفظها في docs/baseline-screens/ بأسماء مرقمة واضحة. سجّل المسارات كلها.

أنشئ docs/HC-P0-S2C-REPORT.md بالنتائج الحرفية: فرق الحارس الجديد، نتيجة كل اختبار من T1 إلى T4 بمن نُفذ ومن لم يُنفذ ولماذا، النداء المتجمد من سجل api إن وقع، التركيبة الخضراء النهائية بحروفها، أرقام E2E التفصيلية، قائمة الفشل التطبيقي المؤجلة، مسارات اللقطات الخمس، وتثبيت حقيقة منع --disable-gpu. حدّث صف P0 في «سجل الحالة الحالي» بالمرجع: VERIFIED فقط إذا نُفذت المجموعة فعليًا والتُقطت اللقطات الخمس، وإلا BLOCKED بالسبب المحدد. اختم بـ«مطابقة التنفيذ مع المرجع» وأسطر نهاية الجلسة الخمسة، واذكر الخطوة التالية الوحيدة بناءً على النتائج من دون تنفيذها.

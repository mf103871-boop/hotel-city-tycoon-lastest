اقرأ docs/HOTEL_CITY_MASTER_REFERENCE_AR.md وdocs/HC-P0-S2A-REPORT.md قبل أي عمل. نفّذ الآن HC-P0-S2B وحدها: عزل انهيار browser context عبر مسبار أدنى خارج مجموعة الاختبارات، بمقارنة headless shell مع Chrome الكامل، ثم فتح الطريق للمجموعة الكاملة واللقطات. ابدأ بقسم «فحص المرجع» ثم قالب الخطة ثم «مطابقة الخطة مع المرجع» قبل التنفيذ. ممنوع تعديل src/ أو data/ أو package-lock.json أو package.json أو منطق أي اختبار في tests/e2e. التعديلات الوحيدة المسموحة: ملف جديد tools/browser-probe.mjs، وحارسان جديدان في playwright.config.ts محددان أدناه، وملف جديد tools/baseline-shots.mjs في حال نجاح الإثبات فقط، وحزم Nix عبر أداة التبعيات إن بلغت المرحلة 3 فقط. ممنوع توليد أي صور أو رسوم أو sprite sheets أو animation.

صفر) فحص ناقص من الخطوة السابقة: نفّذ ldd على ملف chrome-headless-shell داخل chromium_headless_shell-1234 في .cache/ms-playwright واحفظ الناتج في docs/HC-P0-S2B-ldd-shell.txt وسجّل عدد not found.

1) أنشئ tools/browser-probe.mjs: سكربت Node يستورد chromium من playwright ويقبل متغيرات بيئة PROBE_CHANNEL وPROBE_HEADED وPROBE_GPU_OFF وPROBE_URL، وينفذ بالترتيب: launch بالخيارات المبنية من المتغيرات مع args تتضمن دائمًا --disable-dev-shm-usage وتضاف --disable-gpu فقط عند PROBE_GPU_OFF=1، ثم newContext ثم newPage ثم goto إلى PROBE_URL أو about:blank إن لم يحدد، ثم screenshot إلى docs/probe-shots/ باسم يصف التركيبة، ثم إغلاق نظيف، ويطبع في كل مرحلة سطر نجاح أو الخطأ الكامل ويخرج برمز 0 أو 1.

2) المصفوفة الدنيا على about:blank أولًا، شغّل كل تركيبة مع DEBUG=pw:browser* واحفظ سجل كل تشغيل في docs/HC-P0-S2B-probe-N.log برقم متسلسل:
التركيبة أ: بلا متغيرات إضافية، أي headless shell الافتراضي.
التركيبة ب: PROBE_CHANNEL=chromium أي Chrome الكامل بوضع headless الجديد.
التركيبة ج: PROBE_CHANNEL=chromium مع PROBE_GPU_OFF=1.
سجّل جدولًا بنتيجة كل تركيبة عند كل مرحلة launch وcontext وpage وgoto وscreenshot. إن نجحت أي تركيبة على about:blank أعد تشغيلها نفسها مع خادم اللعبة: شغّل بالخلفية HOST=127.0.0.1 PORT=5000 VITE_E2E=1 npm run dev وانتظر جاهزية http://127.0.0.1:5000 ثم PROBE_URL=http://127.0.0.1:5000 واحفظ اللقطة docs/probe-shots/proof-app.png. أوقف الخادم بعدها.

3) فقط إذا فشلت كل تركيبات المرحلة 2 على about:blank: ثبّت عبر أداة التبعيات حزمة xvfb-run وسجّل الاسم الذي نجح، ثم أعد التركيبة ب مسبوقة بـxvfb-run -a ومع PROBE_HEADED=1. إن نجح عالحادةblank كرر مع خادم اللعبة كما في المرحلة 2. إن فشل هذا أيضًا فتوقف نهائيًا وسجّل BLOCKED مع كل السجلات، واذكر في التقرير خيار المالك: نقل E2E إلى بيئة تشغيل خارجية مثل GitHub Actions قرارًا مؤجلًا له.

4) عند أول تركيبة تنجح على اللعبة الحية: أضف في playwright.config.ts داخل use حارسين حرفيين من دون أي تغيير آخر:

...(process.env.PLAYWRIGHT_FULL_CHROMIUM === '1'
  ? { channel: 'chromium' }
  : {}),
...(process.env.PLAYWRIGHT_HEADED === '1'
  ? { headless: false }
  : {}),

ثم شغّل اختبار الإثبات npx playwright test --project=desktop -g "boots past the loading screen" مسبوقًا بمتغيرات البيئة المطابقة للتركيبة الناجحة، ومع xvfb-run -a قبل الأمر إن كانت التركيبة الناجحة من المرحلة 3. سجّل الناتج الحرفي.

5) بعد نجاح الإثبات: شغّل npm run test:e2e كاملًا بنفس المتغيرات والصياغة، وسجّل حرفيًا: المخطط 82، المنفذ فعليًا، الناجح والفاشل لكل مشروع من desktop وphone، المدة، وأسماء كل الاختبارات الفاشلة. ممنوع إصلاح أي فشل تطبيقي؛ أضف كل فشل إلى جدول المؤجل بمعرّف جديد ووصف وخطورة ومرحلة مقترحة.

6) أغلق HC-P0-S1-BL-002: أنشئ tools/baseline-shots.mjs يشغّل الخادم بنفس إعدادات webServer ويلتقط من الجلسة الحية بنفس تركيبة النجاح لقطات الشاشة الرئيسية ولوحة البناء ومتجر الديكور ولوحة الإدارة وعرض هاتف بمقاس Pixel 7، ويحفظها في docs/baseline-screens/ بأسماء مرقمة واضحة. سجّل المسارات.

أنشئ docs/HC-P0-S2B-REPORT.md بالنتائج الحرفية: ناتج ldd للshell، جدول المصفوفة كاملًا بكل تركيبة ومرحلة، سبب النجاح أو الفشل بالسجل، أي حزم أضيفت، فرق playwright.config.ts الكامل، نتيجة الإثبات، أرقام E2E التفصيلية، قائمة الفشل التطبيقي المؤجلة، ومسارات اللقطات. حدّث صف P0 في «سجل الحالة الحالي» بالمرجع: VERIFIED فقط إذا نُفذت المجموعة فعليًا والتُقطت اللقطات الخمس، وإلا BLOCKED بالسبب المحدد وبالتركيبة الأبعد وصولًا. اختم بـ«مطابقة التنفيذ مع المرجع» وأسطر نهاية الجلسة الخمسة، واذكر الخطوة التالية الوحيدة بناءً على النتائج من دون تنفيذها.
